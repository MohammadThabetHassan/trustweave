{
  "classification_counts": {
    "defensive": 3,
    "equivalent": 160,
    "needs_regression": 0
  },
  "classification_policy": "Every record preserves the mutmut source diff. Equivalence requires a recorded code-level proof of semantic redundancy or unreachable control flow; all potentially observable changes remain needs_regression.",
  "mutation_run": "9,692 killed / 10,324 generated (93.88% killed); gated scope 7722 of 7885 (97.93% killed)",
  "schema_version": "v1",
  "survivor_count": 163,
  "survivors": [
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x__read_source__mutmut_18: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -7,7 +7,7 @@\n     if size > MAX_SOURCE_FILE_BYTES:\n         return SkippedFile(relative, \"file_exceeds_size_limit\")\n     try:\n-        text = path.read_text(encoding=\"utf-8\")\n+        text = path.read_text(encoding=\"UTF-8\")\n     except UnicodeDecodeError:\n         return SkippedFile(relative, \"file_is_not_utf8\")\n     except OSError as error:\n",
      "id": "trustweave.code_sources.x__read_source__mutmut_18",
      "rationale": "Semantically redundant. \"UTF-8\" and \"utf-8\" resolve through the codec registry to the same codec, so the decode behaviour and therefore the SourceFile text and the file_is_not_utf8 skip decision are bit-for-bit identical. The behavioural form of this mutation, dropping the encoding so the platform locale is used, is killed by test_source_files_are_read_as_utf8_regardless_of_the_platform_locale.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x__walk_python_files__mutmut_4: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -10,7 +10,7 @@\n \n     discovered: list[Path] = []\n     linked: list[Path] = []\n-    for directory, subdirectories, filenames in os.walk(root, followlinks=False):\n+    for directory, subdirectories, filenames in os.walk(root, followlinks=None):\n         current = Path(directory)\n         subdirectories[:] = sorted(\n             name\n",
      "id": "trustweave.code_sources.x__walk_python_files__mutmut_4",
      "rationale": "Semantically redundant. `followlinks` is tested for truth by os.walk, and None is falsy, so None and False select the same behaviour.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x__walk_python_files__mutmut_6: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -10,7 +10,7 @@\n \n     discovered: list[Path] = []\n     linked: list[Path] = []\n-    for directory, subdirectories, filenames in os.walk(root, followlinks=False):\n+    for directory, subdirectories, filenames in os.walk(root, ):\n         current = Path(directory)\n         subdirectories[:] = sorted(\n             name\n",
      "id": "trustweave.code_sources.x__walk_python_files__mutmut_6",
      "rationale": "Semantically redundant. Omitting `followlinks` restates os.walk's documented default, which is False.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x__walk_python_files__mutmut_7: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -10,7 +10,7 @@\n \n     discovered: list[Path] = []\n     linked: list[Path] = []\n-    for directory, subdirectories, filenames in os.walk(root, followlinks=False):\n+    for directory, subdirectories, filenames in os.walk(root, followlinks=True):\n         current = Path(directory)\n         subdirectories[:] = sorted(\n             name\n",
      "id": "trustweave.code_sources.x__walk_python_files__mutmut_7",
      "rationale": "Unreachable effect. `followlinks` governs whether os.walk descends into a directory symlink, and the next statement removes every symlinked name from `subdirectories` in place, which os.walk honours when topdown is true. No directory symlink is ever offered for descent, so the flag's value cannot change which files are walked.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_48: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(None)\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_48",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_49: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(None, \"path_escapes_analyzed_root\"))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_49",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_50: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(candidate.name, None))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_50",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_51: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(\"path_escapes_analyzed_root\"))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_51",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_52: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(candidate.name, ))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_52",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_53: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(candidate.name, \"XXpath_escapes_analyzed_rootXX\"))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_53",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_54: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -39,7 +39,7 @@\n     for candidate in candidates:\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n-            skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n+            skipped.append(SkippedFile(candidate.name, \"PATH_ESCAPES_ANALYZED_ROOT\"))\n             continue\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_54",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_sources.x_collect_python_sources__mutmut_55: survived\n--- src/trustweave/code_sources.py\n+++ src/trustweave/code_sources.py\n@@ -40,7 +40,7 @@\n         # Defence in depth: a resolved child must still live under the resolved root.\n         if not candidate.resolve().is_relative_to(resolved_root):\n             skipped.append(SkippedFile(candidate.name, \"path_escapes_analyzed_root\"))\n-            continue\n+            break\n         entry = _read_source(candidate, resolved_root)\n         if isinstance(entry, SkippedFile):\n             skipped.append(entry)\n",
      "id": "trustweave.code_sources.x_collect_python_sources__mutmut_55",
      "rationale": "Unreachable control flow. The branch runs only when a candidate's resolved path is not under the resolved root, and `_walk_python_files` cannot produce such a candidate: it diverts every symlinked file into `linked` and removes every symlinked directory from `subdirectories` before os.walk can descend, so each candidate is a regular file reached through real directories under the resolved root and its `resolve()` stays there. The branch is the defence in depth its comment calls it, and no input reaches it, so no mutation of it is observable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.engine.x__reject_near_miss_classifications__mutmut_14: survived\n--- src/trustweave/engine.py\n+++ src/trustweave/engine.py\n@@ -26,7 +26,7 @@\n         declared = source.data_classification\n         if declared in taxonomy:\n             continue\n-        match = folded.get(declared.casefold().strip())\n+        match = None\n         if match is None:\n             close = get_close_matches(declared.casefold(), list(folded), n=1, cutoff=0.85)\n             match = folded[close[0]] if close else None\n",
      "id": "trustweave.engine.x__reject_near_miss_classifications__mutmut_14",
      "rationale": "The exact case-folded lookup is replaced by a constant None, so control always falls through to get_close_matches on the same folded value. A value whose case-fold equals a taxonomy entry has similarity 1.0 against that entry, which is above the 0.85 cutoff, so the fallback returns the identical match the direct lookup would have produced. No input can reach the two branches with different results.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.engine.x__reject_near_miss_classifications__mutmut_15: survived\n--- src/trustweave/engine.py\n+++ src/trustweave/engine.py\n@@ -26,7 +26,7 @@\n         declared = source.data_classification\n         if declared in taxonomy:\n             continue\n-        match = folded.get(declared.casefold().strip())\n+        match = folded.get(None)\n         if match is None:\n             close = get_close_matches(declared.casefold(), list(folded), n=1, cutoff=0.85)\n             match = folded[close[0]] if close else None\n",
      "id": "trustweave.engine.x__reject_near_miss_classifications__mutmut_15",
      "rationale": "folded.get(None) can never hit: every key in folded is a case-folded taxonomy string and None is not a string. The behaviour is therefore identical to mutmut_14, and the get_close_matches fallback recovers the same match for the same reason.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.engine.x__reject_near_miss_classifications__mutmut_24: survived\n--- src/trustweave/engine.py\n+++ src/trustweave/engine.py\n@@ -28,7 +28,7 @@\n             continue\n         match = folded.get(declared.casefold().strip())\n         if match is None:\n-            close = get_close_matches(declared.casefold(), list(folded), n=1, cutoff=0.85)\n+            close = get_close_matches(declared.casefold(), list(folded), cutoff=0.85)\n             match = folded[close[0]] if close else None\n         if match is not None:\n             suspect.append((declared, match))\n",
      "id": "trustweave.engine.x__reject_near_miss_classifications__mutmut_24",
      "rationale": "Dropping n=1 restores difflib's default of 3. Only close[0] is read, and get_close_matches returns matches sorted by descending similarity, so the first element is the same regardless of how many are returned.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.engine.x__reject_near_miss_classifications__mutmut_27: survived\n--- src/trustweave/engine.py\n+++ src/trustweave/engine.py\n@@ -28,7 +28,7 @@\n             continue\n         match = folded.get(declared.casefold().strip())\n         if match is None:\n-            close = get_close_matches(declared.casefold(), list(folded), n=1, cutoff=0.85)\n+            close = get_close_matches(declared.casefold(), list(folded), n=2, cutoff=0.85)\n             match = folded[close[0]] if close else None\n         if match is not None:\n             suspect.append((declared, match))\n",
      "id": "trustweave.engine.x__reject_near_miss_classifications__mutmut_27",
      "rationale": "n=2 instead of n=1, with the same argument as mutmut_24: the result list is ordered by descending similarity and only its first element is consumed.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_vocabulary__mutmut_37: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -24,7 +24,7 @@\n     unknown = sorted(set(declared_sensitive) - set(taxonomy))\n     if unknown:\n         raise ValidationError(\n-            \"chain_manifest.sensitive_classifications must name classifications from \"\n+            \"XXchain_manifest.sensitive_classifications must name classifications from XX\"\n             f\"chain_manifest.classification_taxonomy: {', '.join(unknown)}\"\n         )\n     return ChainVocabulary(taxonomy, frozenset(declared_sensitive))\n",
      "id": "trustweave.chain.x__parse_vocabulary__mutmut_37",
      "rationale": "Only the literal text of a raised message was changed. The condition that raises it, the exception type, and every value the function returns are untouched, so no caller can distinguish the mutant by anything but the wording of the message.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_3: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -14,7 +14,7 @@\n     if value in vocabulary.taxonomy:\n         return\n     folded = {entry.casefold(): entry for entry in vocabulary.taxonomy}\n-    match = folded.get(value.casefold())\n+    match = None\n     if match is None:\n         close = get_close_matches(value.casefold(), list(folded), n=1, cutoff=0.85)\n         match = folded[close[0]] if close else None\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_3",
      "rationale": "Semantically redundant. `folded` is keyed by casefolded taxonomy value, so when `folded.get(value.casefold())` finds an entry, that same key is an exact string in `list(folded)` and difflib scores it 1.0 -- above the 0.85 cutoff and ranked first. Losing the direct lookup therefore only defers to a search that returns the same entry, and both paths reach the same `match`.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_4: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -14,7 +14,7 @@\n     if value in vocabulary.taxonomy:\n         return\n     folded = {entry.casefold(): entry for entry in vocabulary.taxonomy}\n-    match = folded.get(value.casefold())\n+    match = folded.get(None)\n     if match is None:\n         close = get_close_matches(value.casefold(), list(folded), n=1, cutoff=0.85)\n         match = folded[close[0]] if close else None\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_4",
      "rationale": "Semantically redundant. `folded` is keyed by casefolded taxonomy value, so when `folded.get(value.casefold())` finds an entry, that same key is an exact string in `list(folded)` and difflib scores it 1.0 -- above the 0.85 cutoff and ranked first. Losing the direct lookup therefore only defers to a search that returns the same entry, and both paths reach the same `match`.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_13: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -16,7 +16,7 @@\n     folded = {entry.casefold(): entry for entry in vocabulary.taxonomy}\n     match = folded.get(value.casefold())\n     if match is None:\n-        close = get_close_matches(value.casefold(), list(folded), n=1, cutoff=0.85)\n+        close = get_close_matches(value.casefold(), list(folded), cutoff=0.85)\n         match = folded[close[0]] if close else None\n     if match is not None:\n         raise ValidationError(\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_13",
      "rationale": "Semantically redundant. `n` caps how many matches difflib returns and the code reads only `close[0]`. The default of 3 leaves the best match first, so the element read is the same one.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_16: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -16,7 +16,7 @@\n     folded = {entry.casefold(): entry for entry in vocabulary.taxonomy}\n     match = folded.get(value.casefold())\n     if match is None:\n-        close = get_close_matches(value.casefold(), list(folded), n=1, cutoff=0.85)\n+        close = get_close_matches(value.casefold(), list(folded), n=2, cutoff=0.85)\n         match = folded[close[0]] if close else None\n     if match is not None:\n         raise ValidationError(\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_16",
      "rationale": "Semantically redundant, for the same reason as dropping `n`: only `close[0]` is read and difflib returns best-first, so raising the cap to 2 cannot change it.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_22: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -22,7 +22,7 @@\n         raise ValidationError(\n             f\"{path} declares a classification the review will not match: {value!r} looks \"\n             f\"like {match!r}. Classification propagation compares exact strings, so this \"\n-            \"would silently propagate nothing. Correct the declaration, or name the value \"\n+            \"XXwould silently propagate nothing. Correct the declaration, or name the value XX\"\n             \"in chain_manifest.classification_taxonomy.\"\n         )\n     warnings.append(\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_22",
      "rationale": "Only the literal text of a raised message was changed. The condition that raises it, the exception type, and every value the function returns are untouched, so no caller can distinguish the mutant by anything but the wording of the message.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__check_declared_classification__mutmut_25: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -23,7 +23,7 @@\n             f\"{path} declares a classification the review will not match: {value!r} looks \"\n             f\"like {match!r}. Classification propagation compares exact strings, so this \"\n             \"would silently propagate nothing. Correct the declaration, or name the value \"\n-            \"in chain_manifest.classification_taxonomy.\"\n+            \"XXin chain_manifest.classification_taxonomy.XX\"\n         )\n     warnings.append(\n         f\"Declared classification {value!r} is not named in the chain manifest's \"\n",
      "id": "trustweave.chain.x__check_declared_classification__mutmut_25",
      "rationale": "Only the literal text of a raised message was changed. The condition that raises it, the exception type, and every value the function returns are untouched, so no caller can distinguish the mutant by anything but the wording of the message.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_chain_manifest__mutmut_64: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -36,7 +36,7 @@\n                 \"fail_closed\",\n                 \"covers_classifications\",\n             },\n-            path,\n+            None,\n         )\n         identifier = _text(node.get(\"id\"), f\"{path}.id\")\n         if identifier in nodes:\n",
      "id": "trustweave.chain.x__parse_chain_manifest__mutmut_64",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_chain_manifest__mutmut_94: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -41,7 +41,7 @@\n         identifier = _text(node.get(\"id\"), f\"{path}.id\")\n         if identifier in nodes:\n             raise ValidationError(f\"chain_manifest.nodes contains duplicate id: {identifier}\")\n-        kind = _text(node.get(\"kind\"), f\"{path}.kind\")\n+        kind = _text(node.get(\"kind\"), None)\n         if kind not in VALID_NODE_KINDS:\n             raise ValidationError(f\"{path}.kind must be one of {sorted(VALID_NODE_KINDS)}\")\n         allowed_fields_by_kind = {\n",
      "id": "trustweave.chain.x__parse_chain_manifest__mutmut_94",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_chain_manifest__mutmut_183: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -61,7 +61,7 @@\n         if classification is not None:\n             classification = _text(classification, f\"{path}.classification\")\n             _check_declared_classification(\n-                classification, f\"{path}.classification\", vocabulary, warnings\n+                classification, None, vocabulary, warnings\n             )\n         action_class = node.get(\"action_class\")\n         if action_class is not None and action_class not in VALID_ACTION_CLASSES:\n",
      "id": "trustweave.chain.x__parse_chain_manifest__mutmut_183",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_chain_manifest__mutmut_214: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -74,7 +74,7 @@\n         covers = tuple(\n             _text(value, f\"{path}.covers_classifications\")\n             for value in _sequence(\n-                node.get(\"covers_classifications\", []), f\"{path}.covers_classifications\"\n+                node.get(\"covers_classifications\", []), None\n             )\n         )\n         for value in covers:\n",
      "id": "trustweave.chain.x__parse_chain_manifest__mutmut_214",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x__parse_chain_manifest__mutmut_224: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -79,7 +79,7 @@\n         )\n         for value in covers:\n             _check_declared_classification(\n-                value, f\"{path}.covers_classifications\", vocabulary, warnings\n+                value, None, vocabulary, warnings\n             )\n         if kind == \"source\" and trust is None:\n             raise ValidationError(f\"{path}.trust is required for source nodes\")\n",
      "id": "trustweave.chain.x__parse_chain_manifest__mutmut_224",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_review_declared_chains__mutmut_31: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -23,7 +23,7 @@\n     findings: list[dict[str, Any]] = []\n     terminals: dict[tuple[str, ...], _TraversalState] = {}\n     budget_name: str | None = \"max_nodes\" if len(nodes) > max_nodes else None\n-    depth_truncated = False\n+    depth_truncated = None\n     edges_traversed = 0\n     states_explored = 0\n     if budget_name is None:\n",
      "id": "trustweave.chain.x_review_declared_chains__mutmut_31",
      "rationale": "Semantically redundant. `depth_truncated` is read in exactly one place, `if budget_name is not None or depth_truncated:`, which tests it for truth. None and False are both falsy, so the initial value cannot change the branch taken, and the flag is never returned or rendered.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_review_declared_chains__mutmut_69: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -53,7 +53,7 @@\n                 state.incomplete_sanitizers,\n             )\n             if identity in seen_states:\n-                continue\n+                break\n             if states_explored >= max_states:\n                 budget_name = \"max_states\"\n                 break\n",
      "id": "trustweave.chain.x_review_declared_chains__mutmut_69",
      "rationale": "Unreachable control flow. A state's identity includes its path, and no two states pushed by this traversal share one. `_parse_chain_manifest` stores successors in a set, so a duplicated declared edge collapses and each node offers every successor once; the walk pushes one state per (state, successor); and the simple-path guard `target not in state.path` keeps a node from reappearing on its own path. Every pushed state therefore carries a distinct path, `identity in seen_states` is never true, and neither what is stored in `seen_states` nor what the branch does on a repeat can affect the traversal.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_review_declared_chains__mutmut_75: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -57,7 +57,7 @@\n             if states_explored >= max_states:\n                 budget_name = \"max_states\"\n                 break\n-            seen_states.add(identity)\n+            seen_states.add(None)\n             states_explored += 1\n             if node.action_class == \"external\":\n                 if state.path not in terminals and len(terminals) >= max_paths:\n",
      "id": "trustweave.chain.x_review_declared_chains__mutmut_75",
      "rationale": "Unreachable control flow. A state's identity includes its path, and no two states pushed by this traversal share one. `_parse_chain_manifest` stores successors in a set, so a duplicated declared edge collapses and each node offers every successor once; the walk pushes one state per (state, successor); and the simple-path guard `target not in state.path` keeps a node from reappearing on its own path. Every pushed state therefore carries a distinct path, `identity in seen_states` is never true, and neither what is stored in `seen_states` nor what the branch does on a repeat can affect the traversal.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_23: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -3,7 +3,7 @@\n \n     paths = _sequence(review.get(\"paths\"), \"chain_review.paths\")\n     findings = _sequence(review.get(\"findings\"), \"chain_review.findings\")\n-    warnings = _sequence(review.get(\"warnings\", []), \"chain_review.warnings\")\n+    warnings = _sequence(review.get(\"warnings\", []), None)\n     truncated = _analysis_was_truncated(findings)\n     lines = [\"# Declared Chain Review\", \"\", \"## Declared paths\", \"\"]\n     if paths:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_23",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_32: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -3,7 +3,7 @@\n \n     paths = _sequence(review.get(\"paths\"), \"chain_review.paths\")\n     findings = _sequence(review.get(\"findings\"), \"chain_review.findings\")\n-    warnings = _sequence(review.get(\"warnings\", []), \"chain_review.warnings\")\n+    warnings = _sequence(review.get(\"warnings\", []), \"XXchain_review.warningsXX\")\n     truncated = _analysis_was_truncated(findings)\n     lines = [\"# Declared Chain Review\", \"\", \"## Declared paths\", \"\"]\n     if paths:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_32",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_33: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -3,7 +3,7 @@\n \n     paths = _sequence(review.get(\"paths\"), \"chain_review.paths\")\n     findings = _sequence(review.get(\"findings\"), \"chain_review.findings\")\n-    warnings = _sequence(review.get(\"warnings\", []), \"chain_review.warnings\")\n+    warnings = _sequence(review.get(\"warnings\", []), \"CHAIN_REVIEW.WARNINGS\")\n     truncated = _analysis_was_truncated(findings)\n     lines = [\"# Declared Chain Review\", \"\", \"## Declared paths\", \"\"]\n     if paths:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_33",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_101: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -36,7 +36,7 @@\n     lines.extend([\"\", \"## Review findings\", \"\"])\n     if findings:\n         lines.extend(\n-            f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n+            f\"- **{_cell(_mapping(finding, None).get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_101",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_104: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -36,7 +36,7 @@\n     lines.extend([\"\", \"## Review findings\", \"\"])\n     if findings:\n         lines.extend(\n-            f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n+            f\"- **{_cell(_mapping(finding, 'XXfindingXX').get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_104",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_105: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -36,7 +36,7 @@\n     lines.extend([\"\", \"## Review findings\", \"\"])\n     if findings:\n         lines.extend(\n-            f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n+            f\"- **{_cell(_mapping(finding, 'FINDING').get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_105",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_111: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -37,7 +37,7 @@\n     if findings:\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n-            f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n+            f\"({_cell(_mapping(finding, None).get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n         )\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_111",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_114: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -37,7 +37,7 @@\n     if findings:\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n-            f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n+            f\"({_cell(_mapping(finding, 'XXfindingXX').get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n         )\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_114",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_115: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -37,7 +37,7 @@\n     if findings:\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n-            f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n+            f\"({_cell(_mapping(finding, 'FINDING').get('severity'))}): \"\n             f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n             for finding in findings\n         )\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_115",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_121: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -38,7 +38,7 @@\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n-            f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n+            f\"{_cell(_mapping(finding, None).get('message'))}\"\n             for finding in findings\n         )\n     else:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_121",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_124: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -38,7 +38,7 @@\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n-            f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n+            f\"{_cell(_mapping(finding, 'XXfindingXX').get('message'))}\"\n             for finding in findings\n         )\n     else:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_124",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.chain.x_render_chain_review__mutmut_125: survived\n--- src/trustweave/chain.py\n+++ src/trustweave/chain.py\n@@ -38,7 +38,7 @@\n         lines.extend(\n             f\"- **{_cell(_mapping(finding, 'finding').get('id'))}** \"\n             f\"({_cell(_mapping(finding, 'finding').get('severity'))}): \"\n-            f\"{_cell(_mapping(finding, 'finding').get('message'))}\"\n+            f\"{_cell(_mapping(finding, 'FINDING').get('message'))}\"\n             for finding in findings\n         )\n     else:\n",
      "id": "trustweave.chain.x_render_chain_review__mutmut_125",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.config.x__enabled_stages__mutmut_8: survived\n--- src/trustweave/config.py\n+++ src/trustweave/config.py\n@@ -3,7 +3,7 @@\n \n     if not isinstance(value, Sequence) or isinstance(value, (str, bytes, bytearray)):\n         raise ValidationError(\"tool.trustweave.enabled_stages must be a list of stage names\")\n-    if not value or len(value) > MAX_ENABLED_STAGES:\n+    if not value or len(value) >= MAX_ENABLED_STAGES:\n         raise ValidationError(\n             \"tool.trustweave.enabled_stages must contain between 1 and \"\n             f\"{MAX_ENABLED_STAGES} stage names\"\n",
      "id": "trustweave.config.x__enabled_stages__mutmut_8",
      "rationale": "The strict stage vocabulary contains fourteen unique values while MAX_ENABLED_STAGES is sixteen. A length-sixteen selection must contain either an unsupported stage or a duplicate, both rejected by later validation; thus no accepted configuration can distinguish the inclusive size bound.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.config.x__enabled_stages__mutmut_15: survived\n--- src/trustweave/config.py\n+++ src/trustweave/config.py\n@@ -8,7 +8,7 @@\n             \"tool.trustweave.enabled_stages must contain between 1 and \"\n             f\"{MAX_ENABLED_STAGES} stage names\"\n         )\n-    stages = tuple(_string(stage, \"tool.trustweave.enabled_stages[]\") for stage in value)\n+    stages = tuple(_string(stage, None) for stage in value)\n     unknown = sorted(set(stages) - VALID_WORKFLOW_STAGES)\n     if unknown:\n         raise ValidationError(\n",
      "id": "trustweave.config.x__enabled_stages__mutmut_15",
      "rationale": "Only the second argument passed to the internal _string(...) helper was changed from the validation-path 'tool.trustweave.enabled_stages[]' to None. That argument is used for error-path/context formatting; it does not affect successful return values or whether validation accepts or rejects input.",
      "regression_assertion": "''\n\n(Changed value is a validation-path/context string only; no public behavior or decision is affected.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.config.x__enabled_stages__mutmut_18: survived\n--- src/trustweave/config.py\n+++ src/trustweave/config.py\n@@ -8,7 +8,7 @@\n             \"tool.trustweave.enabled_stages must contain between 1 and \"\n             f\"{MAX_ENABLED_STAGES} stage names\"\n         )\n-    stages = tuple(_string(stage, \"tool.trustweave.enabled_stages[]\") for stage in value)\n+    stages = tuple(_string(stage, \"XXtool.trustweave.enabled_stages[]XX\") for stage in value)\n     unknown = sorted(set(stages) - VALID_WORKFLOW_STAGES)\n     if unknown:\n         raise ValidationError(\n",
      "id": "trustweave.config.x__enabled_stages__mutmut_18",
      "rationale": "Only the validation-path/context string passed to _string(...) was altered. This only affects wording of internal error-path context, not validation success/failure or returned data.",
      "regression_assertion": "''\n\n(Only error-path/context text changed; no public decision or result changes.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.config.x__enabled_stages__mutmut_19: survived\n--- src/trustweave/config.py\n+++ src/trustweave/config.py\n@@ -8,7 +8,7 @@\n             \"tool.trustweave.enabled_stages must contain between 1 and \"\n             f\"{MAX_ENABLED_STAGES} stage names\"\n         )\n-    stages = tuple(_string(stage, \"tool.trustweave.enabled_stages[]\") for stage in value)\n+    stages = tuple(_string(stage, \"TOOL.TRUSTWEAVE.ENABLED_STAGES[]\") for stage in value)\n     unknown = sorted(set(stages) - VALID_WORKFLOW_STAGES)\n     if unknown:\n         raise ValidationError(\n",
      "id": "trustweave.config.x__enabled_stages__mutmut_19",
      "rationale": "Only the path/context string argument to _string(...) was changed (case/formatting of the path). That cannot change validation outcomes or returned values, only error message content.",
      "regression_assertion": "''\n\n(Only error-path/context text changed; no public decision or result changes.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.config.x_load_project_config__mutmut_70: survived\n--- src/trustweave/config.py\n+++ src/trustweave/config.py\n@@ -38,5 +38,5 @@\n                 raise ValidationError(f\"{path_name} must be a boolean\")\n             result[key] = value\n         else:  # pragma: no cover - reject_unknown_fields guards the contract above.\n-            raise ValidationError(f\"tool.trustweave: unsupported field {key!r}\")\n+            raise ValidationError(None)\n     return result\n",
      "id": "trustweave.config.x_load_project_config__mutmut_70",
      "rationale": "The `else` branch is statically unreachable because `reject_unknown_fields` runs before the configuration loop and raises for every key outside CONFIG_FIELDS. The changed exception payload cannot be observed on a reachable configuration.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__validate_text__mutmut_8: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -8,4 +8,4 @@\n     # shared. A finding carries text straight into Markdown rows; a newline or a NUL in it\n     # closes the row and lets the remainder of the value write structure of its own.\n     if _CONTROL_CHARACTER_PATTERN.search(value):\n-        raise ValueError(f\"canonical finding {path} must not contain control characters\")\n+        raise ValueError(None)\n",
      "id": "trustweave.findings.x__validate_text__mutmut_8",
      "rationale": "Only the literal text of a raised message was changed. The condition that raises it, the exception type, and every value the function returns are untouched, so no caller can distinguish the mutant by anything but the wording of the message.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__freeze_metadata_mapping__mutmut_14: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -17,7 +17,7 @@\n             raise ValueError(f\"canonical finding {path} keys must use lower_snake_case\")\n         item = value[key]\n         if isinstance(item, str):\n-            _validate_text(item, f\"{path}.{key}\")\n+            _validate_text(item, None)\n             frozen[key] = item\n         elif allow_boolean and isinstance(item, bool):\n             frozen[key] = item\n",
      "id": "trustweave.findings.x__freeze_metadata_mapping__mutmut_14",
      "rationale": "Only the path argument passed to _validate_text was changed (from f\"{path}.{key}\" to None). The path parameter is used for contextualizing error messages; it does not affect validation logic or successful results. Therefore this cannot change whether validation succeeds or the serialized/frozen output.",
      "regression_assertion": "The changed argument is only used for error context; no regression assertion necessary."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__freeze_metadata_mapping__mutmut_43: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -39,7 +39,7 @@\n                 )\n             normalized = tuple(item) if key in _ORDERED_SEQUENCE_FIELDS else tuple(sorted(item))\n             for entry in normalized:\n-                _validate_text(entry, f\"{path}.{key}[]\")\n+                _validate_text(entry, None)\n             frozen[key] = normalized\n         else:\n             raise ValueError(f\"canonical finding {path}.{key} has an unsupported value type\")\n",
      "id": "trustweave.findings.x__freeze_metadata_mapping__mutmut_43",
      "rationale": "Only the path argument passed to _validate_text for each entry (f\"{path}.{key}[]\") was changed to None. That argument only affects error message content; it does not change validation logic or the normalized output, so it cannot affect the public validation outcome.",
      "regression_assertion": "The changed argument is only used for error context; no regression assertion necessary."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__freeze_references__mutmut_13: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -10,7 +10,7 @@\n             entry,\n             \"references[]\",\n             _MAX_SUBJECT_FIELDS,\n-            allow_boolean=False,\n+            allow_boolean=None,\n             allow_integer=False,\n             allow_sequences=False,\n         )\n",
      "id": "trustweave.findings.x__freeze_references__mutmut_13",
      "rationale": "The three option values are consumed only as truth conditions in `_freeze_metadata_mapping`. Replacing literal `False` with `None` leaves each condition falsy, so reference acceptance, rejection, and serialization are identical for all inputs.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__freeze_references__mutmut_14: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -11,7 +11,7 @@\n             \"references[]\",\n             _MAX_SUBJECT_FIELDS,\n             allow_boolean=False,\n-            allow_integer=False,\n+            allow_integer=None,\n             allow_sequences=False,\n         )\n         for entry in value\n",
      "id": "trustweave.findings.x__freeze_references__mutmut_14",
      "rationale": "The three option values are consumed only as truth conditions in `_freeze_metadata_mapping`. Replacing literal `False` with `None` leaves each condition falsy, so reference acceptance, rejection, and serialization are identical for all inputs.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x__freeze_references__mutmut_15: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -12,7 +12,7 @@\n             _MAX_SUBJECT_FIELDS,\n             allow_boolean=False,\n             allow_integer=False,\n-            allow_sequences=False,\n+            allow_sequences=None,\n         )\n         for entry in value\n     )\n",
      "id": "trustweave.findings.x__freeze_references__mutmut_15",
      "rationale": "The three option values are consumed only as truth conditions in `_freeze_metadata_mapping`. Replacing literal `False` with `None` leaves each condition falsy, so reference acceptance, rejection, and serialization are identical for all inputs.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x_parse_finding__mutmut_57: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -20,7 +20,7 @@\n     if unknown:\n         raise ValueError(f\"canonical finding has unknown fields: {', '.join(unknown)}\")\n     return LocalFinding(\n-        identifier=cast(str, document.get(\"id\")),\n+        identifier=cast(None, document.get(\"id\")),\n         severity=cast(str, document.get(\"severity\")),\n         message=cast(str, document.get(\"message\")),\n         evidence_kind=cast(str, document.get(\"evidence_kind\")),\n",
      "id": "trustweave.findings.x_parse_finding__mutmut_57",
      "rationale": "Only the typing.cast target was changed from cast(str, ...) to cast(None, ...). typing.cast is a runtime no-op (it returns the value unchanged), so this change cannot affect the function's runtime result or validation behavior.",
      "regression_assertion": "[]() no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x_parse_finding__mutmut_64: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -21,7 +21,7 @@\n         raise ValueError(f\"canonical finding has unknown fields: {', '.join(unknown)}\")\n     return LocalFinding(\n         identifier=cast(str, document.get(\"id\")),\n-        severity=cast(str, document.get(\"severity\")),\n+        severity=cast(None, document.get(\"severity\")),\n         message=cast(str, document.get(\"message\")),\n         evidence_kind=cast(str, document.get(\"evidence_kind\")),\n         subject=document.get(\"subject\", {}),\n",
      "id": "trustweave.findings.x_parse_finding__mutmut_64",
      "rationale": "Only the typing.cast type argument for severity was changed to None. typing.cast does not affect runtime behavior, so this mutation cannot change public results or validation decisions.",
      "regression_assertion": "[]() no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x_parse_finding__mutmut_71: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -22,7 +22,7 @@\n     return LocalFinding(\n         identifier=cast(str, document.get(\"id\")),\n         severity=cast(str, document.get(\"severity\")),\n-        message=cast(str, document.get(\"message\")),\n+        message=cast(None, document.get(\"message\")),\n         evidence_kind=cast(str, document.get(\"evidence_kind\")),\n         subject=document.get(\"subject\", {}),\n         location=document.get(\"location\"),\n",
      "id": "trustweave.findings.x_parse_finding__mutmut_71",
      "rationale": "Only the typing.cast type argument for message was changed to None. casting is a static hint and has no runtime effect, so the mutation cannot change returned values or validation.",
      "regression_assertion": "[]() no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.findings.x_parse_finding__mutmut_78: survived\n--- src/trustweave/findings.py\n+++ src/trustweave/findings.py\n@@ -23,7 +23,7 @@\n         identifier=cast(str, document.get(\"id\")),\n         severity=cast(str, document.get(\"severity\")),\n         message=cast(str, document.get(\"message\")),\n-        evidence_kind=cast(str, document.get(\"evidence_kind\")),\n+        evidence_kind=cast(None, document.get(\"evidence_kind\")),\n         subject=document.get(\"subject\", {}),\n         location=document.get(\"location\"),\n         references=document.get(\"references\", ()),\n",
      "id": "trustweave.findings.x_parse_finding__mutmut_78",
      "rationale": "Only the typing.cast type argument for evidence_kind was changed to None. typing.cast is a runtime no-op; therefore this mutation cannot alter public results or validation decisions.",
      "regression_assertion": "[]() no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x__fingerprint__mutmut_7: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -1,3 +1,3 @@\n def _fingerprint(kind: str, identifier: str, message: str, artifact_uri: str) -> str:\n     material = \"\\0\".join((kind, identifier, message, artifact_uri))\n-    return sha256(material.encode(\"utf-8\")).hexdigest()\n+    return sha256(material.encode(\"UTF-8\")).hexdigest()\n",
      "id": "trustweave.sarif.x__fingerprint__mutmut_7",
      "rationale": "The only change is the case of the encoding name from 'utf-8' to 'UTF-8'. Python codec names are case-insensitive and both refer to the same encoding; this cannot affect the produced digest or any public behavior.",
      "regression_assertion": "Encoding names are case-insensitive in Python; changing 'utf-8' to 'UTF-8' does not alter the produced hash or observable behavior."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_176: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -75,7 +75,7 @@\n                 results_by_fingerprint[canonical_fingerprint] = result\n                 continue\n \n-            locations = cast(list[dict[str, Any]], result[\"locations\"])\n+            locations = cast(None, result[\"locations\"])\n             if location not in locations:\n                 locations.append(location)\n             properties = cast(dict[str, Any], result[\"properties\"])\n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_176",
      "rationale": "The change only alters the first argument to typing.cast (the static type annotation expression). typing.cast is a no-op at runtime and returns the original value unchanged, so this cannot affect runtime behavior, validation decisions, serialized fields, or ordering of results.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_185: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -78,7 +78,7 @@\n             locations = cast(list[dict[str, Any]], result[\"locations\"])\n             if location not in locations:\n                 locations.append(location)\n-            properties = cast(dict[str, Any], result[\"properties\"])\n+            properties = cast(None, result[\"properties\"])\n             source_kinds = cast(list[str], properties[\"trustweaveSourceKinds\"])\n             if kind not in source_kinds:\n                 source_kinds.append(kind)\n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_185",
      "rationale": "Only the type argument to typing.cast was changed. typing.cast performs no runtime behavior and does not modify the value, so this cannot change public results or validation outcomes.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_192: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -79,7 +79,7 @@\n             if location not in locations:\n                 locations.append(location)\n             properties = cast(dict[str, Any], result[\"properties\"])\n-            source_kinds = cast(list[str], properties[\"trustweaveSourceKinds\"])\n+            source_kinds = cast(None, properties[\"trustweaveSourceKinds\"])\n             if kind not in source_kinds:\n                 source_kinds.append(kind)\n \n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_192",
      "rationale": "This replaces a static typing argument to typing.cast with a different expression. typing.cast is a runtime no-op, so there is no effect on behavior, ordering, serialization, or validation.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_204: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -85,7 +85,7 @@\n \n     results = list(results_by_fingerprint.values())\n     for result in results:\n-        locations = cast(list[dict[str, Any]], result[\"locations\"])\n+        locations = cast(None, result[\"locations\"])\n         locations.sort(\n             key=lambda location: str(location[\"physicalLocation\"][\"artifactLocation\"][\"uri\"])\n         )\n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_204",
      "rationale": "Only the type parameter passed to typing.cast was changed. casting is a static typing aid with no runtime effect; the subsequent sort and data access remain unchanged, so public output cannot be affected.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_222: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -89,7 +89,7 @@\n         locations.sort(\n             key=lambda location: str(location[\"physicalLocation\"][\"artifactLocation\"][\"uri\"])\n         )\n-        properties = cast(dict[str, Any], result[\"properties\"])\n+        properties = cast(None, result[\"properties\"])\n         source_kinds = cast(list[str], properties[\"trustweaveSourceKinds\"])\n         source_kinds.sort()\n \n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_222",
      "rationale": "The mutation only alters the type argument to typing.cast when extracting properties. typing.cast does nothing at runtime, so this change cannot affect the produced SARIF properties or their ordering.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_229: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -90,7 +90,7 @@\n             key=lambda location: str(location[\"physicalLocation\"][\"artifactLocation\"][\"uri\"])\n         )\n         properties = cast(dict[str, Any], result[\"properties\"])\n-        source_kinds = cast(list[str], properties[\"trustweaveSourceKinds\"])\n+        source_kinds = cast(None, properties[\"trustweaveSourceKinds\"])\n         source_kinds.sort()\n \n     results.sort(\n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_229",
      "rationale": "Only the static typing argument of typing.cast was changed for source_kinds. Because typing.cast is a runtime no-op, this cannot change behavior, ordering, or serialized fields.",
      "regression_assertion": "N/A: equivalent change (static type annotation only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.sarif.x_build_sarif__mutmut_247: survived\n--- src/trustweave/sarif.py\n+++ src/trustweave/sarif.py\n@@ -97,7 +97,7 @@\n         key=lambda result: (\n             str(result[\"ruleId\"]),\n             str(result[\"message\"][\"text\"]),\n-            str(result[\"locations\"][0][\"physicalLocation\"][\"artifactLocation\"][\"uri\"]),\n+            str(None),\n         )\n     )\n     rules = [rules_by_id[identifier] for identifier in sorted(rules_by_id)]\n",
      "id": "trustweave.sarif.x_build_sarif__mutmut_247",
      "rationale": "The third SARIF sort key is reached only when rule ID and rendered message are equal. Rendered messages include the review kind, and there is exactly one artifact URI per kind in the input mapping; equal first two keys therefore imply the same URI. Replacing that unreachable tie-breaker cannot alter result ordering.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.evidence.x__chain_digest_v1_or_v2__mutmut_7: survived\n--- src/trustweave/evidence.py\n+++ src/trustweave/evidence.py\n@@ -2,4 +2,4 @@\n     schema_version: str, bundle_hash: str, test_hash: str, source_revision: str\n ) -> str:\n     chain_input = \"|\".join([schema_version, bundle_hash, test_hash, source_revision])\n-    return sha256(chain_input.encode(\"utf-8\")).hexdigest()\n+    return sha256(chain_input.encode(\"UTF-8\")).hexdigest()\n",
      "id": "trustweave.evidence.x__chain_digest_v1_or_v2__mutmut_7",
      "rationale": "Only the encoding name string was changed from 'utf-8' to 'UTF-8'. Encoding names are case-insensitive in Python and this does not change the bytes produced or the resulting digest, so public outputs are unchanged.",
      "regression_assertion": "[]() => no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.evidence.x__chain_digest_v3__mutmut_13: survived\n--- src/trustweave/evidence.py\n+++ src/trustweave/evidence.py\n@@ -6,4 +6,4 @@\n         \"predicate\": predicate,\n         \"subject\": list(subjects),\n     }\n-    return sha256(canonical_json(material).encode(\"utf-8\")).hexdigest()\n+    return sha256(canonical_json(material).encode(\"UTF-8\")).hexdigest()\n",
      "id": "trustweave.evidence.x__chain_digest_v3__mutmut_13",
      "rationale": "Only the encoding name string was changed from 'utf-8' to 'UTF-8'. Encoding names are case-insensitive in Python and this does not change the bytes produced or the resulting digest, so public outputs are unchanged.",
      "regression_assertion": "[]() => no-op"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.evidence.x__logical_name__mutmut_6: survived\n--- src/trustweave/evidence.py\n+++ src/trustweave/evidence.py\n@@ -8,8 +8,7 @@\n         not name\n         or \"/\" in name\n         or \"\\\\\" in name\n-        or candidate.is_absolute()\n-        or len(candidate.parts) != 1\n+        or candidate.is_absolute() and len(candidate.parts) != 1\n         or name in {\".\", \"..\"}\n     ):\n         raise ValidationError(f\"{role} logical artifact name must be one relative file name\")\n",
      "id": "trustweave.evidence.x__logical_name__mutmut_6",
      "rationale": "The new separator guards reject every portable absolute or multi-component logical name before the mutated conjunction is relevant. A remaining non-separator one-component name is not absolute and has exactly one component, so both conditions are false; no accepted or rejected logical artifact name can distinguish the expressions.",
      "regression_assertion": "No regression assertion is required: separator regressions exercise the reachable platform distinction, while this combined predicate is source-proven semantically redundant."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.evidence.x__logical_name__mutmut_18: survived\n--- src/trustweave/evidence.py\n+++ src/trustweave/evidence.py\n@@ -10,7 +10,7 @@\n         or \"\\\\\" in name\n         or candidate.is_absolute()\n         or len(candidate.parts) != 1\n-        or name in {\".\", \"..\"}\n+        or name in {\"XX.XX\", \"..\"}\n     ):\n         raise ValidationError(f\"{role} logical artifact name must be one relative file name\")\n     return name\n",
      "id": "trustweave.evidence.x__logical_name__mutmut_18",
      "rationale": "`Path('.')` has zero components, so the independent `len(candidate.parts) != 1` predicate rejects the dot logical name even if its explicit literal comparison is mutated. The same diagnostic is raised for every reachable input.",
      "regression_assertion": "No regression assertion is required: the adjacent structural predicate provides a code-level proof that this changed literal cannot affect validation behavior."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.evidence.x__verify_v1alpha3_attestation__mutmut_75: survived\n--- src/trustweave/evidence.py\n+++ src/trustweave/evidence.py\n@@ -35,7 +35,7 @@\n             return False\n         name = file_binding.get(\"name\")\n         digest = file_binding.get(\"sha256\")\n-        if not isinstance(name, str) or not _valid_digest(digest):\n+        if not isinstance(name, str) and not _valid_digest(digest):\n             return False\n         if subjects_by_name.get(name) != digest:\n             return False\n",
      "id": "trustweave.evidence.x__verify_v1alpha3_attestation__mutmut_75",
      "rationale": "Subject bindings are populated only after a preceding validation loop that admits string names and valid digests. A non-string exact-file name either fails the original guard or cannot match the string-keyed subject map in the following comparison; an invalid digest still fails both forms of the guard. The AND mutation cannot make a malformed binding verify.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_predicates.x__bounds_cover__mutmut_18: survived\n--- src/trustweave/policy_predicates.py\n+++ src/trustweave/policy_predicates.py\n@@ -38,7 +38,7 @@\n     first_upper = (\n         ranks[first.source_data_classification_at_most]\n         if first.source_data_classification_at_most is not None\n-        else len(ranks) - 1\n+        else len(ranks) + 1\n     )\n     if later.source_data_classifications:\n         # The later rule pins its classification to an exact set; any bound it also states\n",
      "id": "trustweave.policy_predicates.x__bounds_cover__mutmut_18",
      "rationale": "Semantically redundant. `first_upper` is only ever compared against `ranks[value]`, and `ranks` enumerates the taxonomy, so no rank can exceed len(ranks) - 1. Raising the open upper bound to len(ranks) + 1 therefore admits no value that len(ranks) - 1 did not already admit.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_discovery.x__drift__mutmut_36: survived\n--- src/trustweave/code_discovery.py\n+++ src/trustweave/code_discovery.py\n@@ -10,7 +10,7 @@\n \n     renames: list[dict[str, str]] = []\n     for name in absent:\n-        close = difflib.get_close_matches(name, missing, n=1, cutoff=0.8)\n+        close = difflib.get_close_matches(name, missing, cutoff=0.8)\n         if close:\n             renames.append({\"declared\": name, \"discovered\": close[0]})\n \n",
      "id": "trustweave.code_discovery.x__drift__mutmut_36",
      "rationale": "Semantically redundant. Only close[0] is read. difflib.get_close_matches returns the highest-scoring matches in descending score order via heapq.nlargest, which is equivalent to sorted(reverse=True)[:n] over a stably ordered input, so the first element is the same match for every n greater than or equal to one. Changing n alters only how many lower-scoring matches are discarded unread.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.code_discovery.x__drift__mutmut_38: survived\n--- src/trustweave/code_discovery.py\n+++ src/trustweave/code_discovery.py\n@@ -10,7 +10,7 @@\n \n     renames: list[dict[str, str]] = []\n     for name in absent:\n-        close = difflib.get_close_matches(name, missing, n=1, cutoff=0.8)\n+        close = difflib.get_close_matches(name, missing, n=2, cutoff=0.8)\n         if close:\n             renames.append({\"declared\": name, \"discovered\": close[0]})\n \n",
      "id": "trustweave.code_discovery.x__drift__mutmut_38",
      "rationale": "Semantically redundant. Only close[0] is read. difflib.get_close_matches returns the highest-scoring matches in descending score order via heapq.nlargest, which is equivalent to sorted(reverse=True)[:n] over a stably ordered input, so the first element is the same match for every n greater than or equal to one. Changing n alters only how many lower-scoring matches are discarded unread.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_contains_control_characters__mutmut_8: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -8,6 +8,6 @@\n     \"\"\"\n \n     return any(\n-        character < \" \" or character == \"\\x7f\" or character in _UNICODE_LINE_SEPARATORS\n+        character < \" \" or character == \"\\x7F\" or character in _UNICODE_LINE_SEPARATORS\n         for character in value\n     )\n",
      "id": "trustweave.models.x_contains_control_characters__mutmut_8",
      "rationale": "Semantically redundant. '\\x7F' and '\\x7f' are the same Python string literal: the case of the hexadecimal digits in an escape is not significant, and both denote U+007F.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_reject_unknown_fields__mutmut_14: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -8,6 +8,6 @@\n                 f\"{type(field).__name__} key {field!r}\"\n             )\n     for field in sorted(set(value) - allowed):\n-        suggestions = get_close_matches(field, sorted(allowed), n=1, cutoff=0.6)\n+        suggestions = get_close_matches(field, sorted(allowed), cutoff=0.6)\n         suggestion = f\"; did you mean {suggestions[0]!r}?\" if suggestions else \"\"\n         raise ValidationError(f\"{path}: unknown field {field!r}{suggestion}\")\n",
      "id": "trustweave.models.x_reject_unknown_fields__mutmut_14",
      "rationale": "The mutation changes only the requested number of close-match suggestions. The implementation always reads `suggestions[0]`, and `get_close_matches` orders the same best suggestion first regardless of whether one, two, or its default count is requested. The public diagnostic is therefore identical for every unknown field.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_reject_unknown_fields__mutmut_15: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -8,6 +8,6 @@\n                 f\"{type(field).__name__} key {field!r}\"\n             )\n     for field in sorted(set(value) - allowed):\n-        suggestions = get_close_matches(field, sorted(allowed), n=1, cutoff=0.6)\n+        suggestions = get_close_matches(field, sorted(allowed), n=1, )\n         suggestion = f\"; did you mean {suggestions[0]!r}?\" if suggestions else \"\"\n         raise ValidationError(f\"{path}: unknown field {field!r}{suggestion}\")\n",
      "id": "trustweave.models.x_reject_unknown_fields__mutmut_15",
      "rationale": "The change only removes the explicit cutoff keyword while leaving n=1; difflib.get_close_matches default cutoff is 0.6, so behavior is unchanged. This is only a formatting/argument-list change and cannot affect the public validation boundary.",
      "regression_assertion": "Removing the explicit cutoff argument does not change behavior because get_close_matches(..., n=1) uses the same default cutoff=0.6; therefore the produced suggestion and ValidationError text remain unchanged."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_reject_unknown_fields__mutmut_17: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -8,6 +8,6 @@\n                 f\"{type(field).__name__} key {field!r}\"\n             )\n     for field in sorted(set(value) - allowed):\n-        suggestions = get_close_matches(field, sorted(allowed), n=1, cutoff=0.6)\n+        suggestions = get_close_matches(field, sorted(allowed), n=2, cutoff=0.6)\n         suggestion = f\"; did you mean {suggestions[0]!r}?\" if suggestions else \"\"\n         raise ValidationError(f\"{path}: unknown field {field!r}{suggestion}\")\n",
      "id": "trustweave.models.x_reject_unknown_fields__mutmut_17",
      "rationale": "The mutation changes only the requested number of close-match suggestions. The implementation always reads `suggestions[0]`, and `get_close_matches` orders the same best suggestion first regardless of whether one, two, or its default count is requested. The public diagnostic is therefore identical for every unknown field.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_manifest__mutmut_203: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -65,7 +65,7 @@\n         _at_most(raw_capabilities, f\"manifest.tools[{index}].capabilities\", MAX_TOOL_CAPABILITIES)\n         capabilities = tuple(\n             validate_capability_pattern(\n-                capability, f\"manifest.tools[{index}].capabilities\", allow_namespace=False\n+                capability, f\"manifest.tools[{index}].capabilities\", allow_namespace=None\n             )\n             for capability in raw_capabilities\n         )\n",
      "id": "trustweave.models.x_parse_manifest__mutmut_203",
      "rationale": "Semantically redundant. `allow_namespace` is tested for truth, and None is falsy, so None and False select the same branch.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_199: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -68,7 +68,7 @@\n         raw_action_classes = _sequence(\n             rule.get(\"tool_action_classes\"), f\"policy.rules[{index}].tool_action_classes\"\n         )\n-        if schema_version == \"trustweave.dev/policy/v1alpha2\":\n+        if schema_version != \"trustweave.dev/policy/v1alpha2\":\n             _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 3)\n             _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 4)\n         source_trust = tuple(\n",
      "id": "trustweave.models.x_parse_policy__mutmut_199",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_200: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -68,7 +68,7 @@\n         raw_action_classes = _sequence(\n             rule.get(\"tool_action_classes\"), f\"policy.rules[{index}].tool_action_classes\"\n         )\n-        if schema_version == \"trustweave.dev/policy/v1alpha2\":\n+        if schema_version == \"XXtrustweave.dev/policy/v1alpha2XX\":\n             _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 3)\n             _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 4)\n         source_trust = tuple(\n",
      "id": "trustweave.models.x_parse_policy__mutmut_200",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_201: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -68,7 +68,7 @@\n         raw_action_classes = _sequence(\n             rule.get(\"tool_action_classes\"), f\"policy.rules[{index}].tool_action_classes\"\n         )\n-        if schema_version == \"trustweave.dev/policy/v1alpha2\":\n+        if schema_version == \"TRUSTWEAVE.DEV/POLICY/V1ALPHA2\":\n             _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 3)\n             _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 4)\n         source_trust = tuple(\n",
      "id": "trustweave.models.x_parse_policy__mutmut_201",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_208: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -69,7 +69,7 @@\n             rule.get(\"tool_action_classes\"), f\"policy.rules[{index}].tool_action_classes\"\n         )\n         if schema_version == \"trustweave.dev/policy/v1alpha2\":\n-            _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 3)\n+            _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 4)\n             _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 4)\n         source_trust = tuple(\n             _string(value, f\"policy.rules[{index}].source_trust\") for value in raw_source_trust\n",
      "id": "trustweave.models.x_parse_policy__mutmut_208",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_215: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -70,7 +70,7 @@\n         )\n         if schema_version == \"trustweave.dev/policy/v1alpha2\":\n             _at_most(raw_source_trust, f\"policy.rules[{index}].source_trust\", 3)\n-            _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 4)\n+            _at_most(raw_action_classes, f\"policy.rules[{index}].tool_action_classes\", 5)\n         source_trust = tuple(\n             _string(value, f\"policy.rules[{index}].source_trust\") for value in raw_source_trust\n         )\n",
      "id": "trustweave.models.x_parse_policy__mutmut_215",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_350: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -136,7 +136,7 @@\n             )\n         )\n         bounded_fields = (\n-            (\"source_trust\", source_trust, 3),\n+            (\"XXsource_trustXX\", source_trust, 3),\n             (\"tool_action_classes\", action_classes, 4),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_350",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_351: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -136,7 +136,7 @@\n             )\n         )\n         bounded_fields = (\n-            (\"source_trust\", source_trust, 3),\n+            (\"SOURCE_TRUST\", source_trust, 3),\n             (\"tool_action_classes\", action_classes, 4),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_351",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_352: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -136,7 +136,7 @@\n             )\n         )\n         bounded_fields = (\n-            (\"source_trust\", source_trust, 3),\n+            (\"source_trust\", source_trust, 4),\n             (\"tool_action_classes\", action_classes, 4),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_352",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_353: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -137,7 +137,7 @@\n         )\n         bounded_fields = (\n             (\"source_trust\", source_trust, 3),\n-            (\"tool_action_classes\", action_classes, 4),\n+            (\"XXtool_action_classesXX\", action_classes, 4),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n             (\"source_identifiers\", source_identifiers, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_353",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_354: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -137,7 +137,7 @@\n         )\n         bounded_fields = (\n             (\"source_trust\", source_trust, 3),\n-            (\"tool_action_classes\", action_classes, 4),\n+            (\"TOOL_ACTION_CLASSES\", action_classes, 4),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n             (\"source_identifiers\", source_identifiers, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_354",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_355: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -137,7 +137,7 @@\n         )\n         bounded_fields = (\n             (\"source_trust\", source_trust, 3),\n-            (\"tool_action_classes\", action_classes, 4),\n+            (\"tool_action_classes\", action_classes, 5),\n             (\"source_data_classifications\", classifications, 32),\n             (\"tool_capabilities\", capabilities, 128),\n             (\"source_identifiers\", source_identifiers, 128),\n",
      "id": "trustweave.models.x_parse_policy__mutmut_355",
      "rationale": "The mutated first-pass bound or diagnostic-field expression is redundant with an unmutated v1alpha2 bound later in parse_policy's bounded_fields loop. The later check applies the same public maximum after conversion, so no oversized declared collection can become accepted. For malformed members, the mutation can only change which validation error is produced; it cannot produce typed policy state or a successful public artifact.",
      "regression_assertion": "The v1alpha2 parser/schema exact-boundary corpus asserts rejection of every oversized collection; the unmutated bounded_fields pass preserves that outcome."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.models.x_parse_policy__mutmut_450: survived\n--- src/trustweave/models.py\n+++ src/trustweave/models.py\n@@ -199,7 +199,7 @@\n                 upper_rank = (\n                     classification_taxonomy.index(classification_at_most)\n                     if classification_at_most is not None\n-                    else len(classification_taxonomy) - 1\n+                    else len(classification_taxonomy) + 1\n                 )\n                 if not any(\n                     lower_rank <= classification_taxonomy.index(value) <= upper_rank\n",
      "id": "trustweave.models.x_parse_policy__mutmut_450",
      "rationale": "The mutation changes the computed upper rank from len(classification_taxonomy) - 1 to len(classification_taxonomy) + 1. Every index returned by classification_taxonomy.index is between 0 and len-1, so the interval membership predicate has the same truth value for every declared taxonomy member. No policy acceptance or coverage result can change.",
      "regression_assertion": "Classification interval tests cover the inclusive taxonomy endpoints; changing the upper rank above every possible index cannot alter membership."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x__rules_by_identifier__mutmut_9: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -5,6 +5,6 @@\n     for item in _sequence(value):\n         rule = _mapping(item)\n         identifier = rule.get(\"id\")\n-        if isinstance(identifier, str) and identifier:\n+        if isinstance(identifier, str) or identifier:\n             indexed[identifier] = rule\n     return indexed\n",
      "id": "trustweave.policy_weakening.x__rules_by_identifier__mutmut_9",
      "rationale": "The classifier indexes only normalized policy-rule changes emitted after parse_policy. Every such rule id is a non-empty str, so `isinstance(identifier, str) and identifier` and `isinstance(identifier, str) or identifier` have the same truth value. The mutant cannot add a non-string or empty identifier on this strict internal contract.",
      "regression_assertion": "tests/test_diff.py policy-weakening payload, combined-delta, and neutral-delta regressions exercise the strict parsed-policy boundary described above."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x__rule_identifiers__mutmut_2: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -4,5 +4,5 @@\n     return [\n         identifier\n         for rule in _sequence(value)\n-        if isinstance((identifier := _mapping(rule).get(\"id\")), str) and identifier\n+        if isinstance((identifier := _mapping(rule).get(\"id\")), str) or identifier\n     ]\n",
      "id": "trustweave.policy_weakening.x__rule_identifiers__mutmut_2",
      "rationale": "Policy rule identifiers are validated as non-empty strings before bundle generation. A truthy non-string identifier is therefore unreachable in a normalized policy delta.",
      "regression_assertion": "tests/test_diff.py structural-policy and category-specific regressions exercise the strict parsed-policy boundary described above."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x__matching_values__mutmut_5: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -2,5 +2,5 @@\n     \"\"\"Return a canonical set-like matching predicate without malformed values.\"\"\"\n \n     return tuple(\n-        sorted(item for item in _sequence(rule.get(field)) if isinstance(item, str) and item)\n+        sorted(item for item in _sequence(rule.get(field)) if isinstance(item, str) or item)\n     )\n",
      "id": "trustweave.policy_weakening.x__matching_values__mutmut_5",
      "rationale": "The policy parser accepts matching collections only as non-empty strings. Generated policy deltas therefore never contain a truthy non-string value, so replacing `and item` with `or item` cannot change a reachable normalized matching predicate.",
      "regression_assertion": "tests/test_diff.py structural-policy and category-specific regressions exercise the strict parsed-policy boundary described above."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x__reordered_overlapping_rule_ids__mutmut_21: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -11,7 +11,7 @@\n     reordered: set[str] = set()\n     common = sorted(set(before_positions) & set(after_positions))\n     for first_id, second_id in combinations(common, 2):\n-        before_order = before_positions[first_id] < before_positions[second_id]\n+        before_order = before_positions[first_id] <= before_positions[second_id]\n         after_order = after_positions[first_id] < after_positions[second_id]\n         if before_order != after_order and _rules_could_overlap(\n             before_rules[first_id], before_rules[second_id]\n",
      "id": "trustweave.policy_weakening.x__reordered_overlapping_rule_ids__mutmut_21",
      "rationale": "Strict policy parsing requires unique rule identifiers, so distinct rule positions cannot be equal. For every compared pair, `<` and `<=` therefore produce the same ordering result.",
      "regression_assertion": "tests/test_diff.py structural-policy and category-specific regressions exercise the strict parsed-policy boundary described above."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x__reordered_overlapping_rule_ids__mutmut_23: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -12,7 +12,7 @@\n     common = sorted(set(before_positions) & set(after_positions))\n     for first_id, second_id in combinations(common, 2):\n         before_order = before_positions[first_id] < before_positions[second_id]\n-        after_order = after_positions[first_id] < after_positions[second_id]\n+        after_order = after_positions[first_id] <= after_positions[second_id]\n         if before_order != after_order and _rules_could_overlap(\n             before_rules[first_id], before_rules[second_id]\n         ):\n",
      "id": "trustweave.policy_weakening.x__reordered_overlapping_rule_ids__mutmut_23",
      "rationale": "Strict policy parsing requires unique rule identifiers, so distinct rule positions cannot be equal. For every compared pair, `<` and `<=` therefore produce the same ordering result.",
      "regression_assertion": "tests/test_diff.py structural-policy and category-specific regressions exercise the strict parsed-policy boundary described above."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x_policy_review_signals__mutmut_83: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -44,7 +44,7 @@\n         )\n \n     approval_changes = {\n-        path.rsplit(\".\", maxsplit=1)[-1]: change\n+        path.rsplit(\".\", )[-1]: change\n         for path, change in changes_by_path.items()\n         if path.startswith(\"policy.approval_control.\")\n     }\n",
      "id": "trustweave.policy_weakening.x_policy_review_signals__mutmut_83",
      "rationale": "Semantically redundant. Only `[-1]` is read. Splitting on every '.' and taking the last element yields the same text as splitting once from the right and taking the last element, for any input.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x_policy_review_signals__mutmut_86: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -44,7 +44,7 @@\n         )\n \n     approval_changes = {\n-        path.rsplit(\".\", maxsplit=1)[-1]: change\n+        path.rsplit(\".\", maxsplit=2)[-1]: change\n         for path, change in changes_by_path.items()\n         if path.startswith(\"policy.approval_control.\")\n     }\n",
      "id": "trustweave.policy_weakening.x_policy_review_signals__mutmut_86",
      "rationale": "Semantically redundant, for the same reason as dropping maxsplit: `[-1]` is the final segment whatever the split count, so maxsplit=2 selects the same text.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x_policy_review_signals__mutmut_87: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -44,7 +44,7 @@\n         )\n \n     approval_changes = {\n-        path.rsplit(\".\", maxsplit=1)[-1]: change\n+        path.rsplit(\".\", maxsplit=1)[+1]: change\n         for path, change in changes_by_path.items()\n         if path.startswith(\"policy.approval_control.\")\n     }\n",
      "id": "trustweave.policy_weakening.x_policy_review_signals__mutmut_87",
      "rationale": "Semantically redundant. The comprehension admits only paths beginning 'policy.approval_control.', so every path it splits contains at least one '.' and `rsplit('.', maxsplit=1)` returns exactly two elements. Index 1 and index -1 are the same element of a two-element list, so the mutation selects the same text and the IndexError it could otherwise raise is unreachable.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x_policy_review_signals__mutmut_228: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -107,7 +107,7 @@\n             if (\n                 isinstance(before_decision, str)\n                 and isinstance(after_decision, str)\n-                and _DECISION_RESTRICTION.get(after_decision, -1)\n+                and _DECISION_RESTRICTION.get(after_decision, -2)\n                 < _DECISION_RESTRICTION.get(before_decision, -1)\n             ):\n                 weakened_decisions.append(identifier)\n",
      "id": "trustweave.policy_weakening.x_policy_review_signals__mutmut_228",
      "rationale": "Semantically redundant. The literal is the rank given to a decision string that _DECISION_RESTRICTION does not name, and it is only ever used in `rank(after) < rank(before)`. Every named rank is 0, 1 or 2, so -2 and -1 order identically against all of them, and when both sides are unnamed the comparison is `d < d`, false for either value. No pair of decisions compares differently.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_weakening.x_policy_review_signals__mutmut_235: survived\n--- src/trustweave/policy_weakening.py\n+++ src/trustweave/policy_weakening.py\n@@ -108,7 +108,7 @@\n                 isinstance(before_decision, str)\n                 and isinstance(after_decision, str)\n                 and _DECISION_RESTRICTION.get(after_decision, -1)\n-                < _DECISION_RESTRICTION.get(before_decision, -1)\n+                < _DECISION_RESTRICTION.get(before_decision, -2)\n             ):\n                 weakened_decisions.append(identifier)\n             before_controls = {\n",
      "id": "trustweave.policy_weakening.x_policy_review_signals__mutmut_235",
      "rationale": "Semantically redundant. The literal is the rank given to a decision string that _DECISION_RESTRICTION does not name, and it is only ever used in `rank(after) < rank(before)`. Every named rank is 0, 1 or 2, so -2 and -1 order identically against all of them, and when both sides are unnamed the comparison is `d < d`, false for either value. No pair of decisions compares differently.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "defensive",
      "diff": "# trustweave.policy_review.x__covering_rules__mutmut_37: survived\n--- src/trustweave/policy_review.py\n+++ src/trustweave/policy_review.py\n@@ -25,7 +25,7 @@\n         return None, [], False\n     used: set[str] = set()\n     for values in product(*domains):\n-        narrowed = {name: (value,) for name, value in zip(fields, values, strict=True)}\n+        narrowed = {name: (value,) for name, value in zip(fields, values, strict=None)}\n         cell = replace(later, **cast(dict[str, Any], narrowed))\n         cover = next((rule for rule in possible if rule_covers(rule, cell, policy)), None)\n         if cover is None:\n",
      "id": "trustweave.policy_review.x__covering_rules__mutmut_37",
      "rationale": "Defensive. `strict=True` asserts that `fields` and `values` have equal length; they are derived from one another in the lines above, so the assertion cannot fire on any input this function is given. Weakening or dropping it removes a guard against a future edit rather than changing behaviour today.",
      "regression_assertion": ""
    },
    {
      "classification": "defensive",
      "diff": "# trustweave.policy_review.x__covering_rules__mutmut_40: survived\n--- src/trustweave/policy_review.py\n+++ src/trustweave/policy_review.py\n@@ -25,7 +25,7 @@\n         return None, [], False\n     used: set[str] = set()\n     for values in product(*domains):\n-        narrowed = {name: (value,) for name, value in zip(fields, values, strict=True)}\n+        narrowed = {name: (value,) for name, value in zip(fields, values, )}\n         cell = replace(later, **cast(dict[str, Any], narrowed))\n         cover = next((rule for rule in possible if rule_covers(rule, cell, policy)), None)\n         if cover is None:\n",
      "id": "trustweave.policy_review.x__covering_rules__mutmut_40",
      "rationale": "Defensive. `strict=True` asserts that `fields` and `values` have equal length; they are derived from one another in the lines above, so the assertion cannot fire on any input this function is given. Weakening or dropping it removes a guard against a future edit rather than changing behaviour today.",
      "regression_assertion": ""
    },
    {
      "classification": "defensive",
      "diff": "# trustweave.policy_review.x__covering_rules__mutmut_41: survived\n--- src/trustweave/policy_review.py\n+++ src/trustweave/policy_review.py\n@@ -25,7 +25,7 @@\n         return None, [], False\n     used: set[str] = set()\n     for values in product(*domains):\n-        narrowed = {name: (value,) for name, value in zip(fields, values, strict=True)}\n+        narrowed = {name: (value,) for name, value in zip(fields, values, strict=False)}\n         cell = replace(later, **cast(dict[str, Any], narrowed))\n         cover = next((rule for rule in possible if rule_covers(rule, cell, policy)), None)\n         if cover is None:\n",
      "id": "trustweave.policy_review.x__covering_rules__mutmut_41",
      "rationale": "Defensive. `strict=True` asserts that `fields` and `values` have equal length; they are derived from one another in the lines above, so the assertion cannot fire on any input this function is given. Weakening or dropping it removes a guard against a future edit rather than changing behaviour today.",
      "regression_assertion": ""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.policy_review.x__covering_rules__mutmut_46: survived\n--- src/trustweave/policy_review.py\n+++ src/trustweave/policy_review.py\n@@ -26,7 +26,7 @@\n     used: set[str] = set()\n     for values in product(*domains):\n         narrowed = {name: (value,) for name, value in zip(fields, values, strict=True)}\n-        cell = replace(later, **cast(dict[str, Any], narrowed))\n+        cell = replace(later, **cast(None, narrowed))\n         cover = next((rule for rule in possible if rule_covers(rule, cell, policy)), None)\n         if cover is None:\n             return None, [], True\n",
      "id": "trustweave.policy_review.x__covering_rules__mutmut_46",
      "rationale": "Semantically redundant. typing.cast performs no runtime conversion and no check; it returns its second argument unchanged, so its first argument cannot affect execution.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__stable_subject__mutmut_4: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -3,7 +3,7 @@\n \n     if value is None:\n         return {}\n-    subject = _mapping(value, path)\n+    subject = _mapping(value, None)\n     normalized: dict[str, Any] = {}\n     for key in sorted(subject):\n         if not isinstance(key, str):\n",
      "id": "trustweave.risk.x__stable_subject__mutmut_4",
      "rationale": "Only the path argument passed into an internal helper (_mapping) was changed from the local 'path' variable to None. This is a validation-path argument used for constructing error messages; it does not change validation logic, returned normalized structure, or serialized fields, so it cannot affect public results except possibly error message contents. Because this is a validation-path-only argument change, classify as equivalent.",
      "regression_assertion": "N/A: equivalent change (validation-path argument only)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__fingerprint__mutmut_14: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -7,4 +7,4 @@\n         \"id\": identifier,\n         \"subject\": subject,\n     }\n-    return sha256(canonical_json(material).encode(\"utf-8\")).hexdigest()\n+    return sha256(canonical_json(material).encode(\"UTF-8\")).hexdigest()\n",
      "id": "trustweave.risk.x__fingerprint__mutmut_14",
      "rationale": "Only the encoding name string for .encode() was changed from \"utf-8\" to \"UTF-8\". Python codec names are case-insensitive and this cannot change validation decisions, canonicalization, or the returned fingerprint digest.",
      "regression_assertion": "Encoding name case is canonical-only; this cannot affect public boundary. No functional change: using 'utf-8' vs 'UTF-8' yields identical bytes and digest."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_normalize_findings__mutmut_55: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -18,7 +18,7 @@\n         identifier = _text(finding.get(\"id\"), f\"{path}.id\")\n         severity = _LEGACY_SEVERITY_MAP.get(\n             _text(finding.get(\"severity\"), f\"{path}.severity\"),\n-            _text(finding.get(\"severity\"), f\"{path}.severity\"),\n+            _text(finding.get(\"severity\"), None),\n         )\n         if severity not in SEVERITY_RANK:\n             raise ValidationError(\n",
      "id": "trustweave.risk.x_normalize_findings__mutmut_55",
      "rationale": "The first `_text` call validates and returns the severity before the mutated second call is reached. For every reachable input, the second call receives the same already-valid value; for invalid input the first unmutated call raises. Its diagnostic-path change is therefore unobservable.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_normalize_findings__mutmut_137: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -42,5 +42,5 @@\n                 remediation=_optional_text(finding.get(\"remediation\"), f\"{path}.remediation\"),\n             )\n         )\n-    _reject_indistinct_findings(findings, collection_name)\n+    _reject_indistinct_findings(findings, None)\n     return tuple(sorted(findings, key=lambda item: (item.fingerprint, item.identifier)))\n",
      "id": "trustweave.risk.x_normalize_findings__mutmut_137",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__reject_indistinct_findings__mutmut_9: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -14,7 +14,7 @@\n         key = (finding.identifier, canonical_json(subject))\n         presentation = (\n             finding.message,\n-            finding.title or \"\",\n+            finding.title or \"XXXX\",\n             finding.rationale or \"\",\n             finding.remediation or \"\",\n         )\n",
      "id": "trustweave.risk.x__reject_indistinct_findings__mutmut_9",
      "rationale": "Semantically redundant. The literal is a sentinel standing in for an absent or empty optional field while two presentations are compared for equality. Every finding in the comparison is folded through the same sentinel in the same tuple position, so two presentations compare equal under the mutant exactly when they compared equal before. Only a sentinel that could collide with a real field value would change an outcome, and the mutant's is further from any real value than the empty string it replaces.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__reject_indistinct_findings__mutmut_11: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -15,7 +15,7 @@\n         presentation = (\n             finding.message,\n             finding.title or \"\",\n-            finding.rationale or \"\",\n+            finding.rationale or \"XXXX\",\n             finding.remediation or \"\",\n         )\n         existing = presentations.setdefault(key, presentation)\n",
      "id": "trustweave.risk.x__reject_indistinct_findings__mutmut_11",
      "rationale": "Semantically redundant. The literal is a sentinel standing in for an absent or empty optional field while two presentations are compared for equality. Every finding in the comparison is folded through the same sentinel in the same tuple position, so two presentations compare equal under the mutant exactly when they compared equal before. Only a sentinel that could collide with a real field value would change an outcome, and the mutant's is further from any real value than the empty string it replaces.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__reject_indistinct_findings__mutmut_13: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -16,7 +16,7 @@\n             finding.message,\n             finding.title or \"\",\n             finding.rationale or \"\",\n-            finding.remediation or \"\",\n+            finding.remediation or \"XXXX\",\n         )\n         existing = presentations.setdefault(key, presentation)\n         if existing != presentation:\n",
      "id": "trustweave.risk.x__reject_indistinct_findings__mutmut_13",
      "rationale": "Semantically redundant. The literal is a sentinel standing in for an absent or empty optional field while two presentations are compared for equality. Every finding in the comparison is folded through the same sentinel in the same tuple position, so two presentations compare equal under the mutant exactly when they compared equal before. Only a sentinel that could collide with a real field value would change an outcome, and the mutant's is further from any real value than the empty string it replaces.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__reject_indistinct_findings__mutmut_22: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -23,5 +23,5 @@\n             raise ValidationError(\n                 f\"artifact.{collection_name} contains two different {finding.identifier} findings \"\n                 f\"with one identity {key[1]}; the risk fingerprint excludes wording, so a \"\n-                \"producer must give each finding a distinguishing subject\"\n+                \"XXproducer must give each finding a distinguishing subjectXX\"\n             )\n",
      "id": "trustweave.risk.x__reject_indistinct_findings__mutmut_22",
      "rationale": "Only the literal text of a raised message was changed. The condition that raises it, the exception type, and every value the function returns are untouched, so no caller can distinguish the mutant by anything but the wording of the message.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__timestamp__mutmut_12: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -1,7 +1,7 @@\n def _timestamp(value: Any, path: str) -> datetime:\n     text = _text(value, path)\n     try:\n-        parsed = datetime.fromisoformat(text.replace(\"Z\", \"+00:00\"))\n+        parsed = datetime.fromisoformat(text.replace(\"XXZXX\", \"+00:00\"))\n     except ValueError as error:\n         raise ValidationError(f\"{path} must be an ISO 8601 timestamp\") from error\n     if parsed.tzinfo is None:\n",
      "id": "trustweave.risk.x__timestamp__mutmut_12",
      "rationale": "The supported Python baseline is 3.11+, where `datetime.fromisoformat` natively accepts an uppercase trailing `Z`. Replacing `Z` before parsing is therefore semantically redundant for every accepted timestamp, while malformed inputs still raise the same validation error.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__timestamp__mutmut_13: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -1,7 +1,7 @@\n def _timestamp(value: Any, path: str) -> datetime:\n     text = _text(value, path)\n     try:\n-        parsed = datetime.fromisoformat(text.replace(\"Z\", \"+00:00\"))\n+        parsed = datetime.fromisoformat(text.replace(\"z\", \"+00:00\"))\n     except ValueError as error:\n         raise ValidationError(f\"{path} must be an ISO 8601 timestamp\") from error\n     if parsed.tzinfo is None:\n",
      "id": "trustweave.risk.x__timestamp__mutmut_13",
      "rationale": "The supported Python baseline is 3.11+, where `datetime.fromisoformat` natively accepts an uppercase trailing `Z`. Replacing `Z` before parsing is therefore semantically redundant for every accepted timestamp, while malformed inputs still raise the same validation error.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__subject_digest__mutmut_6: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -1,4 +1,4 @@\n def _subject_digest(subject: Mapping[str, Any]) -> str:\n     \"\"\"Bind a decision to the normalized subject identity without retaining mutable content.\"\"\"\n \n-    return sha256(canonical_json(dict(subject)).encode(\"utf-8\")).hexdigest()\n+    return sha256(canonical_json(dict(subject)).encode(\"UTF-8\")).hexdigest()\n",
      "id": "trustweave.risk.x__subject_digest__mutmut_6",
      "rationale": "Only the encoding name string for .encode() changed from \"utf-8\" to \"UTF-8\". Codec names are case-insensitive in Python, so the produced bytes and SHA256 digest are identical and public outputs cannot change.",
      "regression_assertion": "Encoding name case is canonical-only; this cannot affect public boundary. No functional change: using 'utf-8' vs 'UTF-8' yields identical bytes and digest."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_4: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -8,7 +8,7 @@\n \n     if document is None:\n         return {}\n-    root = _mapping(document, collection_name)\n+    root = _mapping(document, None)\n     document_schema_version = root.get(\"schema_version\")\n     if document_schema_version == legacy_schema_version:\n         raise ValidationError(\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_4",
      "rationale": "Only the 'path' argument passed to the internal helper _mapping was changed from collection_name to None. This only affects the path used in error messages (validation-path context) and does not change validation logic, return values, or deterministic outputs.",
      "regression_assertion": "\"\"\u00a0\n\nNote: change only affects error-path text; no assertion needed."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_59: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -34,7 +34,7 @@\n     for index, raw_entry in enumerate(_sequence(root.get(collection_name), collection_name)):\n         path = f\"{collection_name}[{index}]\"\n         entry = _mapping(raw_entry, path)\n-        reject_unknown_fields(entry, required, path)\n+        reject_unknown_fields(entry, required, None)\n         fingerprint = _text(entry.get(\"fingerprint\"), f\"{path}.fingerprint\")\n         if len(fingerprint) != 64 or any(\n             character not in \"0123456789abcdef\" for character in fingerprint\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_59",
      "rationale": "Only the path argument given to reject_unknown_fields was changed from the concrete path to None. That only changes which path appears in error messages; it cannot alter validation decisions or public outputs.",
      "regression_assertion": "\"\"\u00a0\n\nNote: change only affects error-path text; no assertion needed."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_65: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -35,7 +35,7 @@\n         path = f\"{collection_name}[{index}]\"\n         entry = _mapping(raw_entry, path)\n         reject_unknown_fields(entry, required, path)\n-        fingerprint = _text(entry.get(\"fingerprint\"), f\"{path}.fingerprint\")\n+        fingerprint = _text(entry.get(\"fingerprint\"), None)\n         if len(fingerprint) != 64 or any(\n             character not in \"0123456789abcdef\" for character in fingerprint\n         ):\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_65",
      "rationale": "Only the path argument passed to the _text helper for the fingerprint field was removed (changed to None). This only affects error message path context and not validation logic or return values.",
      "regression_assertion": "\"\"\u00a0\n\nNote: change only affects error-path text; no assertion needed."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_86: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -44,7 +44,7 @@\n             raise ValidationError(\n                 f\"{path}.fingerprint_schema_version must be {FINGERPRINT_SCHEMA_VERSION}\"\n             )\n-        rule_id = validate_rule_identifier(entry.get(\"rule_id\"), f\"{path}.rule_id\")\n+        rule_id = validate_rule_identifier(entry.get(\"rule_id\"), None)\n         if not rule_id.startswith(\"TW-\"):\n             raise ValidationError(f\"{path}.rule_id must begin with TW-\")\n         subject_digest = _text(entry.get(\"subject_digest\"), f\"{path}.subject_digest\")\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_86",
      "rationale": "Only the path argument passed into validate_rule_identifier was changed from the per-entry path to None. That affects only the error/path context and not the validation semantics or outputs.",
      "regression_assertion": "\"\"\u00a0\n\nNote: change only affects error-path text; no assertion needed."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_99: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -47,7 +47,7 @@\n         rule_id = validate_rule_identifier(entry.get(\"rule_id\"), f\"{path}.rule_id\")\n         if not rule_id.startswith(\"TW-\"):\n             raise ValidationError(f\"{path}.rule_id must begin with TW-\")\n-        subject_digest = _text(entry.get(\"subject_digest\"), f\"{path}.subject_digest\")\n+        subject_digest = _text(entry.get(\"subject_digest\"), None)\n         if len(subject_digest) != 64 or any(\n             character not in \"0123456789abcdef\" for character in subject_digest\n         ):\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_99",
      "rationale": "Only the path argument passed to _text for subject_digest was changed to None. This only changes error-path text and cannot change validation results or serialized outputs.",
      "regression_assertion": "\"\"\u00a0\n\nNote: change only affects error-path text; no assertion needed."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_115: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -52,7 +52,7 @@\n             character not in \"0123456789abcdef\" for character in subject_digest\n         ):\n             raise ValidationError(f\"{path}.subject_digest must be a SHA-256 hex digest\")\n-        accepted_severity = _text(entry.get(\"accepted_severity\"), f\"{path}.accepted_severity\")\n+        accepted_severity = _text(entry.get(\"accepted_severity\"), None)\n         if accepted_severity not in SEVERITY_RANK:\n             raise ValidationError(\n                 f\"{path}.accepted_severity must be one of {list(VALID_SEVERITIES)}\"\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_115",
      "rationale": "Only the second argument (error/path label) passed to the _text(...) validator was changed from a path string to None. This only affects the validation error path/message, not whether validation succeeds or the validated value returned, so it cannot change the public result or validation decision.",
      "regression_assertion": "''\n\n(Equivalent change: no regression assertion required.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_168: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -69,7 +69,7 @@\n         entries[fingerprint] = RiskDecision(\n             fingerprint=fingerprint,\n             accepted_severity=accepted_severity,\n-            reason=_text(entry.get(\"reason\"), f\"{path}.reason\"),\n+            reason=_text(entry.get(\"reason\"), None),\n             owner=_text(entry.get(\"owner\"), f\"{path}.owner\"),\n             created_at=created_at,\n             expires_at=expires_at,\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_168",
      "rationale": "Only the validation-path argument passed to _text(...) for the reason field was changed from a path string to None. That affects only error messages, not the validated value or whether validation passes.",
      "regression_assertion": "''\n\n(Equivalent change: no regression assertion required.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x__decision_entries__mutmut_175: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -70,7 +70,7 @@\n             fingerprint=fingerprint,\n             accepted_severity=accepted_severity,\n             reason=_text(entry.get(\"reason\"), f\"{path}.reason\"),\n-            owner=_text(entry.get(\"owner\"), f\"{path}.owner\"),\n+            owner=_text(entry.get(\"owner\"), None),\n             created_at=created_at,\n             expires_at=expires_at,\n             rule_id=rule_id,\n",
      "id": "trustweave.risk.x__decision_entries__mutmut_175",
      "rationale": "Only the validation-path argument passed to _text(...) for the owner field was changed from a path string to None. This affects only error/reporting text and cannot change the validated owner value returned for valid input.",
      "regression_assertion": "''\n\n(Equivalent change: no regression assertion required.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_36: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -15,7 +15,7 @@\n     normalized_owner = _text(owner, \"baseline.owner\")\n     created = _timestamp(created_at, \"baseline.created_at\")\n     expiry = _timestamp(expires_at, \"baseline.expires_at\")\n-    review_timestamp = _timestamp(review.get(\"generated_at\"), \"risk_review.generated_at\")\n+    review_timestamp = _timestamp(review.get(\"generated_at\"), None)\n     if created < review_timestamp:\n         raise ValidationError(\"baseline.created_at must not precede review timestamp\")\n     if expiry <= created:\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_36",
      "rationale": "Only the field name argument passed into _timestamp was changed from \"risk_review.generated_at\" to None. That argument is used for validation context/error messages; it does not change timestamp parsing or the success/failure of validation itself, so it cannot change successful public results or whether validation succeeds.",
      "regression_assertion": "\"\"\"\"\"\"\"\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_42: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -15,7 +15,7 @@\n     normalized_owner = _text(owner, \"baseline.owner\")\n     created = _timestamp(created_at, \"baseline.created_at\")\n     expiry = _timestamp(expires_at, \"baseline.expires_at\")\n-    review_timestamp = _timestamp(review.get(\"generated_at\"), \"risk_review.generated_at\")\n+    review_timestamp = _timestamp(review.get(\"generated_at\"), \"XXrisk_review.generated_atXX\")\n     if created < review_timestamp:\n         raise ValidationError(\"baseline.created_at must not precede review timestamp\")\n     if expiry <= created:\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_42",
      "rationale": "Only the field-name string passed to _timestamp was changed to a different literal. This only affects the phrasing of any validation error context and does not affect behavior or validation decisions.",
      "regression_assertion": "\"\"\"\"\"\"\"\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_43: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -15,7 +15,7 @@\n     normalized_owner = _text(owner, \"baseline.owner\")\n     created = _timestamp(created_at, \"baseline.created_at\")\n     expiry = _timestamp(expires_at, \"baseline.expires_at\")\n-    review_timestamp = _timestamp(review.get(\"generated_at\"), \"risk_review.generated_at\")\n+    review_timestamp = _timestamp(review.get(\"generated_at\"), \"RISK_REVIEW.GENERATED_AT\")\n     if created < review_timestamp:\n         raise ValidationError(\"baseline.created_at must not precede review timestamp\")\n     if expiry <= created:\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_43",
      "rationale": "Only the field-name string passed into _timestamp was changed (capitalization and punctuation). This is a validation-path argument change affecting only error context text, not validation outcome or public results.",
      "regression_assertion": "\"\"\"\"\"\"\"\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_63: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -23,7 +23,7 @@\n     normalized_reference = _optional_text(reference, \"baseline.reference\")\n     entries: list[dict[str, str]] = []\n     seen: set[str] = set()\n-    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"risk_review.findings\")):\n+    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), None)):\n         path = f\"risk_review.findings[{index}]\"\n         finding = _mapping(raw_finding, path)\n         state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_63",
      "rationale": "Only the second argument passed to _sequence was changed from the path string \"risk_review.findings\" to None. This is a validation-path argument used for error messages and does not affect validation logic or successful return values.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path string passed to _sequence used in error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_69: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -23,7 +23,7 @@\n     normalized_reference = _optional_text(reference, \"baseline.reference\")\n     entries: list[dict[str, str]] = []\n     seen: set[str] = set()\n-    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"risk_review.findings\")):\n+    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"XXrisk_review.findingsXX\")):\n         path = f\"risk_review.findings[{index}]\"\n         finding = _mapping(raw_finding, path)\n         state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_69",
      "rationale": "Only the validation-path string passed to _sequence was changed (\"risk_review.findings\" -> \"XXrisk_review.findingsXX\"). This affects only error-path text, not validation decisions or returned data.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path string passed to _sequence used in error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_70: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -23,7 +23,7 @@\n     normalized_reference = _optional_text(reference, \"baseline.reference\")\n     entries: list[dict[str, str]] = []\n     seen: set[str] = set()\n-    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"risk_review.findings\")):\n+    for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"RISK_REVIEW.FINDINGS\")):\n         path = f\"risk_review.findings[{index}]\"\n         finding = _mapping(raw_finding, path)\n         state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_70",
      "rationale": "Only the validation-path string passed to _sequence was changed (\"risk_review.findings\" -> \"RISK_REVIEW.FINDINGS\"). This is purely a path/label for error messages and does not change validation behavior or outputs.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path string passed to _sequence used in error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_74: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -25,7 +25,7 @@\n     seen: set[str] = set()\n     for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"risk_review.findings\")):\n         path = f\"risk_review.findings[{index}]\"\n-        finding = _mapping(raw_finding, path)\n+        finding = _mapping(raw_finding, None)\n         state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n         if state not in ACTIVE_RISK_STATES:\n             continue\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_74",
      "rationale": "Only the second argument to _mapping was changed from the path variable to None. This is a validation-path argument for error messages and does not alter validation logic or return values.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path argument passed to _mapping used for error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_79: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -26,7 +26,7 @@\n     for index, raw_finding in enumerate(_sequence(review.get(\"findings\"), \"risk_review.findings\")):\n         path = f\"risk_review.findings[{index}]\"\n         finding = _mapping(raw_finding, path)\n-        state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n+        state = _text(finding.get(\"risk_state\"), None)\n         if state not in ACTIVE_RISK_STATES:\n             continue\n         fingerprint = _text(finding.get(\"fingerprint\"), f\"{path}.fingerprint\")\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_79",
      "rationale": "Only the path argument passed to _text was replaced with None. That argument is used for error-path text; the change does not affect validation decisions or successful outputs.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path argument passed to _text used for error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_89: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -29,7 +29,7 @@\n         state = _text(finding.get(\"risk_state\"), f\"{path}.risk_state\")\n         if state not in ACTIVE_RISK_STATES:\n             continue\n-        fingerprint = _text(finding.get(\"fingerprint\"), f\"{path}.fingerprint\")\n+        fingerprint = _text(finding.get(\"fingerprint\"), None)\n         if len(fingerprint) != 64 or any(\n             character not in \"0123456789abcdef\" for character in fingerprint\n         ):\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_89",
      "rationale": "Only the path argument passed to _text for the fingerprint was changed to None. This is strictly the validation-path used in error messages and does not change the acceptance or normalization logic.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path argument passed to _text used for error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_110: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -38,7 +38,7 @@\n             raise ValidationError(\n                 f\"{path}.fingerprint_schema_version must be {FINGERPRINT_SCHEMA_VERSION}\"\n             )\n-        accepted_severity = _text(finding.get(\"severity\"), f\"{path}.severity\")\n+        accepted_severity = _text(finding.get(\"severity\"), None)\n         if accepted_severity not in SEVERITY_RANK:\n             raise ValidationError(f\"{path}.severity must be one of {list(VALID_SEVERITIES)}\")\n         subject = _stable_subject(finding.get(\"subject\"), f\"{path}.subject\")\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_110",
      "rationale": "Only the path argument to _text for severity was changed to None. This alteration only changes the error-path label and cannot affect validation logic or the produced baseline.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path argument passed to _text used for error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.risk.x_create_baseline__mutmut_121: survived\n--- src/trustweave/risk.py\n+++ src/trustweave/risk.py\n@@ -41,7 +41,7 @@\n         accepted_severity = _text(finding.get(\"severity\"), f\"{path}.severity\")\n         if accepted_severity not in SEVERITY_RANK:\n             raise ValidationError(f\"{path}.severity must be one of {list(VALID_SEVERITIES)}\")\n-        subject = _stable_subject(finding.get(\"subject\"), f\"{path}.subject\")\n+        subject = _stable_subject(finding.get(\"subject\"), None)\n         if not subject:\n             raise ValidationError(f\"{path}.subject must bind a v1alpha2 decision\")\n         if fingerprint not in seen:\n",
      "id": "trustweave.risk.x_create_baseline__mutmut_121",
      "rationale": "Only the path argument passed to _stable_subject was changed to None. This is only used for error messages (validation-path) and does not modify validation decisions or output binding behavior.",
      "regression_assertion": "Change only a validation-path argument; cannot affect whether validation succeeds or the produced baseline. The mutation only alters the path argument passed to _stable_subject used for error reporting."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__config_path__mutmut_4: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -3,7 +3,7 @@\n \n     if args.config is not None:\n         if not isinstance(args.config, Path):\n-            raise ValidationError(\"--config must identify a local configuration path\")\n+            raise ValidationError(\"XX--config must identify a local configuration pathXX\")\n         return args.config\n     if args.no_config_discovery:\n         raise ValidationError(\"--no-config-discovery requires an explicit --config path\")\n",
      "id": "trustweave.commands.ci.x__config_path__mutmut_4",
      "rationale": "Only the ValidationError message text was changed; the control flow and validation decision (raising ValidationError when args.config is not a Path) are unchanged. The mutation only affects the textual error message in the validation path and cannot change whether validation succeeds or the successful result.",
      "regression_assertion": "The change is limited to the validation-path exception message and does not alter validation behavior."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__config_path__mutmut_7: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -6,5 +6,5 @@\n             raise ValidationError(\"--config must identify a local configuration path\")\n         return args.config\n     if args.no_config_discovery:\n-        raise ValidationError(\"--no-config-discovery requires an explicit --config path\")\n+        raise ValidationError(\"XX--no-config-discovery requires an explicit --config pathXX\")\n     return find_project_config(Path.cwd())\n",
      "id": "trustweave.commands.ci.x__config_path__mutmut_7",
      "rationale": "This mutation only changes the string passed to ValidationError when args.no_config_discovery is true and no config is provided. It does not alter control flow or the validation decision, only the exception message text.",
      "regression_assertion": "The change is limited to the validation-path exception message and does not alter validation behavior."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__selected_stages__mutmut_11: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -5,7 +5,7 @@\n     if configured is None:\n         return DEFAULT_STAGES\n     if not isinstance(configured, tuple) or not all(isinstance(stage, str) for stage in configured):\n-        raise ValidationError(\"tool.trustweave.enabled_stages must be a validated stage list\")\n+        raise ValidationError(\"XXtool.trustweave.enabled_stages must be a validated stage listXX\")\n     unsupported = sorted(set(configured) - CI_SUPPORTED_STAGES)\n     if unsupported:\n         raise ValidationError(\n",
      "id": "trustweave.commands.ci.x__selected_stages__mutmut_11",
      "rationale": "Only the ValidationError message text for an invalid configured value was changed. The type check and raising of ValidationError remain the same, so successful results and validation decisions are unchanged; only the error string is affected.",
      "regression_assertion": "The change is limited to the validation-path exception message and does not alter validation behavior."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__validate_stage_dependencies__mutmut_28: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -8,7 +8,7 @@\n             raise ValidationError(f\"{stage} stage requires selected stages: {missing}\")\n     if \"risk\" in selected and not (selected & REVIEW_STAGES):\n         raise ValidationError(\"risk stage requires at least one selected local review stage\")\n-    if \"sarif\" in selected and not (\"risk\" in selected or selected & REVIEW_STAGES):\n+    if \"sarif\" in selected and not (\"XXriskXX\" in selected or selected & REVIEW_STAGES):\n         raise ValidationError(\n             \"sarif stage requires risk or at least one selected local review stage\"\n         )\n",
      "id": "trustweave.commands.ci.x__validate_stage_dependencies__mutmut_28",
      "rationale": "The only possible distinguishing selection is `{risk, sarif}` without a review stage, but the preceding risk prerequisite rejects that set before the SARIF predicate executes. Whenever the SARIF predicate is reached with `risk`, either a review stage is also selected or the prior guard has already failed; the changed literal is unreachable.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__validate_stage_dependencies__mutmut_29: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -8,7 +8,7 @@\n             raise ValidationError(f\"{stage} stage requires selected stages: {missing}\")\n     if \"risk\" in selected and not (selected & REVIEW_STAGES):\n         raise ValidationError(\"risk stage requires at least one selected local review stage\")\n-    if \"sarif\" in selected and not (\"risk\" in selected or selected & REVIEW_STAGES):\n+    if \"sarif\" in selected and not (\"RISK\" in selected or selected & REVIEW_STAGES):\n         raise ValidationError(\n             \"sarif stage requires risk or at least one selected local review stage\"\n         )\n",
      "id": "trustweave.commands.ci.x__validate_stage_dependencies__mutmut_29",
      "rationale": "The only possible distinguishing selection is `{risk, sarif}` without a review stage, but the preceding risk prerequisite rejects that set before the SARIF predicate executes. Whenever the SARIF predicate is reached with `risk`, either a review stage is also selected or the prior guard has already failed; the changed literal is unreachable.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__safe_sarif_path__mutmut_23: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -7,8 +7,7 @@\n     path = Path(configured)\n     windows_path = PureWindowsPath(configured)\n     if (\n-        path.is_absolute()\n-        or windows_path.is_absolute()\n+        path.is_absolute() and windows_path.is_absolute()\n         or windows_path.drive\n         or windows_path.root\n         or \"..\" in path.parts\n",
      "id": "trustweave.commands.ci.x__safe_sarif_path__mutmut_23",
      "rationale": "The mutated absolute/traversal predicates are redundant with later disjuncts in the same guard: a Windows root is independently rejected by `windows_path.root`, and a `..` component in a portable Path is also present in `PureWindowsPath(configured).parts`. The same containment exception is raised for every reachable input.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__safe_sarif_path__mutmut_24: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -11,7 +11,7 @@\n         or windows_path.is_absolute()\n         or windows_path.drive\n         or windows_path.root\n-        or \"..\" in path.parts\n+        or \"XX..XX\" in path.parts\n         or \"..\" in windows_path.parts\n         or path == Path(\".\")\n     ):\n",
      "id": "trustweave.commands.ci.x__safe_sarif_path__mutmut_24",
      "rationale": "The mutated absolute/traversal predicates are redundant with later disjuncts in the same guard: a Windows root is independently rejected by `windows_path.root`, and a `..` component in a portable Path is also present in `PureWindowsPath(configured).parts`. The same containment exception is raised for every reachable input.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__publish_directory__mutmut_13: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -10,7 +10,7 @@\n     backup = output.parent / f\".{output.name}.previous\"\n     if backup.exists():\n         shutil.rmtree(backup)\n-    replaced = False\n+    replaced = None\n     try:\n         if output.exists():\n             output.replace(backup)\n",
      "id": "trustweave.commands.ci.x__publish_directory__mutmut_13",
      "rationale": "`backup` is removed before publication. If an output existed, both original and mutant set `replaced` true immediately after moving it to backup; if it did not, no backup remains for restoration. Consequently the altered initial flag and restoration boolean cannot change the only reachable recovery states.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__publish_directory__mutmut_14: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -10,7 +10,7 @@\n     backup = output.parent / f\".{output.name}.previous\"\n     if backup.exists():\n         shutil.rmtree(backup)\n-    replaced = False\n+    replaced = True\n     try:\n         if output.exists():\n             output.replace(backup)\n",
      "id": "trustweave.commands.ci.x__publish_directory__mutmut_14",
      "rationale": "replaced is initialised to True. The initial value can only matter if an exception is raised before the assignment while the conjunction's remaining operands hold. The backup directory is removed immediately above, so at that point backup.exists() is False and the guard is False under either initialisation.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__publish_directory__mutmut_20: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -17,7 +17,7 @@\n             replaced = True\n         staging.replace(output)\n     except OSError as error:\n-        if replaced and backup.exists() and not output.exists():\n+        if replaced or backup.exists() and not output.exists():\n             backup.replace(output)\n         raise InputOutputError(f\"Could not publish CI artifacts to {output}: {error}\") from error\n     if backup.exists():\n",
      "id": "trustweave.commands.ci.x__publish_directory__mutmut_20",
      "rationale": "The guard becomes `replaced or (backup.exists() and not output.exists())`. When replaced is False the backup does not exist, so the added disjunct is False; when replaced is True the backup exists and output has been moved away, so both operands of the original conjunction are True. The two expressions agree on every reachable state.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/\"\""
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__review_findings__mutmut_9: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -1,7 +1,7 @@\n def _review_findings(review: Mapping[str, Any]) -> list[Mapping[str, Any]]:\n     \"\"\"Return canonical review findings or diff signals from one selected local artifact.\"\"\"\n \n-    raw = review.get(\"findings\", review.get(\"signals\", []))\n+    raw = review.get(\"findings\", review.get(\"signals\", None))\n     if not isinstance(raw, list):\n         return []\n     return [finding for finding in raw if isinstance(finding, Mapping)]\n",
      "id": "trustweave.commands.ci.x__review_findings__mutmut_9",
      "rationale": "The mutation alters the inner default from [] to None (review.get('signals', None)). In the calling code, if both 'findings' and 'signals' are absent raw becomes None, and the subsequent isinstance(raw, list) check causes the function to return [] just like the original. For reviews that include 'signals' the value is returned identically. Therefore the change cannot affect the public return value or validation outcome.",
      "regression_assertion": "The change only affects an internal default value; downstream the code rejects non-lists and returns [], so there is no change to the function's public return. (No regression assertion needed beyond verifying the function returns the same list results for inputs with and without 'findings'/'signals'.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x__review_findings__mutmut_11: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -1,7 +1,7 @@\n def _review_findings(review: Mapping[str, Any]) -> list[Mapping[str, Any]]:\n     \"\"\"Return canonical review findings or diff signals from one selected local artifact.\"\"\"\n \n-    raw = review.get(\"findings\", review.get(\"signals\", []))\n+    raw = review.get(\"findings\", review.get(\"signals\", ))\n     if not isinstance(raw, list):\n         return []\n     return [finding for finding in raw if isinstance(finding, Mapping)]\n",
      "id": "trustweave.commands.ci.x__review_findings__mutmut_11",
      "rationale": "The mutation omits the inner default in review.get('signals', ) which makes that inner get default to None. As with other variants, when both 'findings' and 'signals' are missing raw becomes None but the subsequent isinstance check returns []\u2014the same public behavior as the original. When 'signals' exists its value is used the same way. Thus this syntactic change does not alter the documented public boundary.",
      "regression_assertion": "The change only affects an internal default evaluation; the function still returns list of mapping results and returns [] for missing or non-list data. (No further public-behavior assertion beyond existing behavior.)"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_180: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -63,7 +63,7 @@\n     with tempfile.TemporaryDirectory(prefix=\".trustweave-ci-\", dir=output_dir.parent) as temporary:\n         staging = Path(temporary) / \"artifacts\"\n         staging.mkdir()\n-        manifest = None\n+        manifest = \"\"\n         policy = None\n         bundle_path: Path | None = None\n         test_path: Path | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_180",
      "rationale": "`manifest` and `policy` are initialized only as lazy stage-local caches and subsequently read through `cache or parse_*`. Both `None` and the mutated empty string are falsy, so every reachable stage parses the same local input and produces identical artifacts.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_181: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -64,7 +64,7 @@\n         staging = Path(temporary) / \"artifacts\"\n         staging.mkdir()\n         manifest = None\n-        policy = None\n+        policy = \"\"\n         bundle_path: Path | None = None\n         test_path: Path | None = None\n         attestation_path: Path | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_181",
      "rationale": "`manifest` and `policy` are initialized only as lazy stage-local caches and subsequently read through `cache or parse_*`. Both `None` and the mutated empty string are falsy, so every reachable stage parses the same local input and produces identical artifacts.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_182: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -65,7 +65,7 @@\n         staging.mkdir()\n         manifest = None\n         policy = None\n-        bundle_path: Path | None = None\n+        bundle_path: Path | None = \"\"\n         test_path: Path | None = None\n         attestation_path: Path | None = None\n         policy_review: Mapping[str, Any] | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_182",
      "rationale": "The mutated path variables are guarded by stage dependencies: attestation requires scan and scenarios, and report requires attestation. A reachable attestation/report therefore replaces these initial values with paths; the only states where the initial value remains are rejected before any public artifact is produced. The altered `is None` behavior is unreachable.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_184: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -67,7 +67,7 @@\n         policy = None\n         bundle_path: Path | None = None\n         test_path: Path | None = None\n-        attestation_path: Path | None = None\n+        attestation_path: Path | None = \"\"\n         policy_review: Mapping[str, Any] | None = None\n         diff_review: Mapping[str, Any] | None = None\n         trace_review: Mapping[str, Any] | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_184",
      "rationale": "The mutated path variables are guarded by stage dependencies: attestation requires scan and scenarios, and report requires attestation. A reachable attestation/report therefore replaces these initial values with paths; the only states where the initial value remains are rejected before any public artifact is produced. The altered `is None` behavior is unreachable.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_185: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -68,7 +68,7 @@\n         bundle_path: Path | None = None\n         test_path: Path | None = None\n         attestation_path: Path | None = None\n-        policy_review: Mapping[str, Any] | None = None\n+        policy_review: Mapping[str, Any] | None = \"\"\n         diff_review: Mapping[str, Any] | None = None\n         trace_review: Mapping[str, Any] | None = None\n         mcp_profile_review: Mapping[str, Any] | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_185",
      "rationale": "These stage-local review variables are assigned only in their selected stage and are not read afterward; `raw_reviews` is the sole source for later review, SARIF, risk, and summary logic. When a stage is absent its initialized value is dead state, so changing `None` to an empty string cannot affect a reachable result.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_186: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -69,7 +69,7 @@\n         test_path: Path | None = None\n         attestation_path: Path | None = None\n         policy_review: Mapping[str, Any] | None = None\n-        diff_review: Mapping[str, Any] | None = None\n+        diff_review: Mapping[str, Any] | None = \"\"\n         trace_review: Mapping[str, Any] | None = None\n         mcp_profile_review: Mapping[str, Any] | None = None\n         chain_review: Mapping[str, Any] | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_186",
      "rationale": "These stage-local review variables are assigned only in their selected stage and are not read afterward; `raw_reviews` is the sole source for later review, SARIF, risk, and summary logic. When a stage is absent its initialized value is dead state, so changing `None` to an empty string cannot affect a reachable result.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_187: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -70,7 +70,7 @@\n         attestation_path: Path | None = None\n         policy_review: Mapping[str, Any] | None = None\n         diff_review: Mapping[str, Any] | None = None\n-        trace_review: Mapping[str, Any] | None = None\n+        trace_review: Mapping[str, Any] | None = \"\"\n         mcp_profile_review: Mapping[str, Any] | None = None\n         chain_review: Mapping[str, Any] | None = None\n         risk_review: Mapping[str, Any] | None = None\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_187",
      "rationale": "These stage-local review variables are assigned only in their selected stage and are not read afterward; `raw_reviews` is the sole source for later review, SARIF, risk, and summary logic. When a stage is absent its initialized value is dead state, so changing `None` to an empty string cannot affect a reachable result.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_188: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -71,7 +71,7 @@\n         policy_review: Mapping[str, Any] | None = None\n         diff_review: Mapping[str, Any] | None = None\n         trace_review: Mapping[str, Any] | None = None\n-        mcp_profile_review: Mapping[str, Any] | None = None\n+        mcp_profile_review: Mapping[str, Any] | None = \"\"\n         chain_review: Mapping[str, Any] | None = None\n         risk_review: Mapping[str, Any] | None = None\n         raw_reviews: dict[str, tuple[str, Mapping[str, Any]]] = {}\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_188",
      "rationale": "These stage-local review variables are assigned only in their selected stage and are not read afterward; `raw_reviews` is the sole source for later review, SARIF, risk, and summary logic. When a stage is absent its initialized value is dead state, so changing `None` to an empty string cannot affect a reachable result.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_189: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -72,7 +72,7 @@\n         diff_review: Mapping[str, Any] | None = None\n         trace_review: Mapping[str, Any] | None = None\n         mcp_profile_review: Mapping[str, Any] | None = None\n-        chain_review: Mapping[str, Any] | None = None\n+        chain_review: Mapping[str, Any] | None = \"\"\n         risk_review: Mapping[str, Any] | None = None\n         raw_reviews: dict[str, tuple[str, Mapping[str, Any]]] = {}\n         artifacts: list[str] = []\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_189",
      "rationale": "These stage-local review variables are assigned only in their selected stage and are not read afterward; `raw_reviews` is the sole source for later review, SARIF, risk, and summary logic. When a stage is absent its initialized value is dead state, so changing `None` to an empty string cannot affect a reachable result.",
      "regression_assertion": "No regression assertion is required: source-level reachability and semantic proof is recorded in the rationale."
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_214: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -83,7 +83,7 @@\n             bundle = build_bundle(manifest, policy, generated_at)\n             # The same round-trip `scan` performs. Without it the staged run could write a\n             # bundle its own attest and diff stages refuse, which is where this bites.\n-            validate_bundle(bundle, \"bundle\")\n+            validate_bundle(bundle, None)\n             bundle_path = write_json(staging / BUNDLE_FILE, bundle)\n             artifacts.append(BUNDLE_FILE)\n         if \"scenarios\" in stages:\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_214",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_216: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -83,7 +83,7 @@\n             bundle = build_bundle(manifest, policy, generated_at)\n             # The same round-trip `scan` performs. Without it the staged run could write a\n             # bundle its own attest and diff stages refuse, which is where this bites.\n-            validate_bundle(bundle, \"bundle\")\n+            validate_bundle(bundle, )\n             bundle_path = write_json(staging / BUNDLE_FILE, bundle)\n             artifacts.append(BUNDLE_FILE)\n         if \"scenarios\" in stages:\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_216",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_217: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -83,7 +83,7 @@\n             bundle = build_bundle(manifest, policy, generated_at)\n             # The same round-trip `scan` performs. Without it the staged run could write a\n             # bundle its own attest and diff stages refuse, which is where this bites.\n-            validate_bundle(bundle, \"bundle\")\n+            validate_bundle(bundle, \"XXbundleXX\")\n             bundle_path = write_json(staging / BUNDLE_FILE, bundle)\n             artifacts.append(BUNDLE_FILE)\n         if \"scenarios\" in stages:\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_217",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_218: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -83,7 +83,7 @@\n             bundle = build_bundle(manifest, policy, generated_at)\n             # The same round-trip `scan` performs. Without it the staged run could write a\n             # bundle its own attest and diff stages refuse, which is where this bites.\n-            validate_bundle(bundle, \"bundle\")\n+            validate_bundle(bundle, \"BUNDLE\")\n             bundle_path = write_json(staging / BUNDLE_FILE, bundle)\n             artifacts.append(BUNDLE_FILE)\n         if \"scenarios\" in stages:\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_218",
      "rationale": "Only the path/context argument used to build an error message was changed. It is carried into message text and nothing else: it takes no part in any predicate, is never returned, and never reaches an artifact. Validation outcomes and returned values are identical, so the mutation is observable only in the wording of a message raised on an input this call does not receive here.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_512: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -181,7 +181,7 @@\n             )\n             artifacts.append(ATTESTATION_FILE)\n         if \"report\" in stages:\n-            if bundle_path is None or test_path is None or attestation_path is None:\n+            if bundle_path is None and test_path is None or attestation_path is None:\n                 raise ValidationError(\n                     \"report stage requires selected scan, scenarios, and attestation stages\"\n                 )\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_512",
      "rationale": "Unreachable control flow. STAGE_DEPENDENCIES declares that 'report' requires 'scan', 'scenarios' and 'attestation', and `_validate_stage_dependencies` raises before any artifact is staged when they are not all selected. By the time this guard runs, all three stages have run and assigned their paths, so none of the three is None and the condition is false however its operators are arranged. The guard is defence in depth against a future stage table, not a branch any input reaches.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_539: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -195,7 +195,7 @@\n \n         test_failed = (\n             test_path is not None\n-            and read_json(test_path).get(\"summary\", {}).get(\"status\") != \"passed\"\n+            and read_json(test_path).get(\"summary\", None).get(\"status\") != \"passed\"\n         )\n         risk_has_active_finding = risk_review is not None and any(\n             isinstance(finding, Mapping) and finding.get(\"risk_state\") in ACTIVE_RISK_STATES\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_539",
      "rationale": "Unreachable control flow. The default is used only when the test-results document has no 'summary' key. That document is not external input: this same function wrote it a few lines earlier from `run_scenarios`, and 'summary' is required by schemas/test-results-v1alpha1.schema.json, which the generated artifact is held to. The mapping read here therefore always carries 'summary' and the default is never taken, so replacing it cannot change the comparison.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    },
    {
      "classification": "equivalent",
      "diff": "# trustweave.commands.ci.x_handle__mutmut_541: survived\n--- src/trustweave/commands/ci.py\n+++ src/trustweave/commands/ci.py\n@@ -195,7 +195,7 @@\n \n         test_failed = (\n             test_path is not None\n-            and read_json(test_path).get(\"summary\", {}).get(\"status\") != \"passed\"\n+            and read_json(test_path).get(\"summary\", ).get(\"status\") != \"passed\"\n         )\n         risk_has_active_finding = risk_review is not None and any(\n             isinstance(finding, Mapping) and finding.get(\"risk_state\") in ACTIVE_RISK_STATES\n",
      "id": "trustweave.commands.ci.x_handle__mutmut_541",
      "rationale": "Unreachable control flow. The default is used only when the test-results document has no 'summary' key. That document is not external input: this same function wrote it a few lines earlier from `run_scenarios`, and 'summary' is required by schemas/test-results-v1alpha1.schema.json, which the generated artifact is held to. The mapping read here therefore always carries 'summary' and the default is never taken, so replacing it cannot change the comparison.",
      "regression_assertion": "/*** no regression assertion required for equivalent mutation ***/"
    }
  ],
  "untriaged_count": 0
}
